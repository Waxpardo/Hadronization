# Handoff v12 — sweep completion and production-shape measurements

**Date:** 2026-08-08. **Branch:** `physics-focus`.
**HEAD at time of writing:** `bd811d0`. **Nikhef HEAD:** `e690e17` — frozen,
**read-only this session** (metadata reads and ROOT over ssh only; nothing
written, nothing submitted, checkout not moved).

**v10 remains current for the registry/physics state. v11 records the previous
sweep session.**

---

## 1. Per-item status

| item | status | commit |
|---|---|---|
| 1 — `DESIGN_AND_RATIONALE` §3.2, §3.12 | **landed** | `c569102` |
| 2 — `REPRODUCIBILITY` (a)(b)(c) | **landed** | `addcb4a` |
| 3 — README | **item-STOP** — already fixed | — |
| 3 — `WORKSPACE` | **landed** | `954617a` |
| 4 — proposal amendments (a)–(e) | **NOT STARTED** | — |
| 5 — pool-RSS measurement | **IN FLIGHT**, detached | — |
| 6 — production-shape measurements | **landed** (iii deferred) | `bd811d0` |
| 7 — `PRODUCTION_SHAPE_DECISION.md` | **landed**, awaiting STOP-AND-ASK | `bd811d0` |
| 8 — blocker reconciliation | **NOT STARTED** | — |
| 9 — this handoff | — | — |

Baseline `40d10a7` → `bd811d0`. **No code, config, generated header, seed
ledger, tune card or `Paper/` content touched.**

---

## 2. Item 3 README — verification failed, nothing written

The audit (`REPO_AUDIT_c1bb0d9.md:587`) cites `README.md:54-56` claiming "Eight
downstream components still assume exactly three tunes". **That text no longer
exists** — grep for `eight|downstream component|assume exactly` returns nothing.
It was already removed, and `README.md:61-62` now documents the `TuneOrdinal`
restriction directly. **The audit's finding is closed; the audit itself is
stale on this row.**

---

## 3. Item 5 — pool-RSS, IN FLIGHT

```
/data/alice/ipardoza/poolrss_run01/   (retained unconditionally)
  rss.log  time_v.txt  stdout.log  RSS_DONE (sentinel; absent = running)
Driver PID 367476, setsid nohup. Script /data/alice/ipardoza/poolrss.sh
```

`/usr/bin/time -v` on one `validate_pair_directory.sh` against the promoted
JUNCTIONS central, read-only, launched uncontended (loadavg 0.54 on 62 cores).
Expect ~640 s.

**Pre-registered interpretation, written before launch:** if the largest child's
maxrss is **~400–460 MB**, the B10 pool table's "fits under the login cap"
claim **inverts** — P=8 needs ~3.5 GiB against 2.00 GiB — and the corrected
sizing is **P≤4 on the login node, or the pool runs on a batch node**.

**Supporting evidence already in hand, from two different tools:** the gate's
P=1 run reported `maxrssKB=451480` (441 MB) while the validator's own `/proc`
showed 18.4 MB; and an earlier `/usr/bin/time -f` on `validate_pair_directory.sh`
reported `maxrssKB=594028` (580 MB). **Both are far above 18.4 MB, so the
hypothesis is already well supported — this run confirms it with full rusage
detail.** Update **both** annotations (B10 and the checklist) from *hypothesis*
to *measured* whichever way it lands.

---

## 4. Item 6 — measurements, with methods

**All: `TChain` over the first 10 jobs per tune = 1,000,000 events per tune,
read-only. Expectations pre-registered before each run.**

**(i) Mixture — pre-registered ~10:1, MEASURED 6.39:1.** Beauty fraction
**13.5 %**, tune-independent (MONASH 6.401, JUNCTIONS 6.382, CLOSEPACKING
6.387). **Beauty is ~35 % more abundant than assumed.**

**(ii) Disjointness verified exactly.** `hard_channel==4` ↔ codes 121/122,
`==5` ↔ 123/124, **zero cross-contamination both ways, all three tunes.**

**(ii) N_ch by channel — beauty at ~2.05x charm.** Means: MONASH 9.675 / 20.172
(2.085), JUNCTIONS 10.685 / 21.725 (2.033), CLOSEPACKING 10.053 / 20.289
(2.018). Pre-registration confirmed; magnitude is a factor of two.

**(ii) Class occupancy — M6 quantified.** 11 equal-population percentile classes
from each tune's own pooled `multiplicity_primary_charged_eta10_v1`:

| | c01 | c11 | swing |
|---|---|---|---|
| MONASH | 3.45 % | **45.31 %** | 13.1x |
| JUNCTIONS | 3.02 % | **44.89 %** | 14.9x |
| CLOSEPACKING | 3.31 % | **43.97 %** | 13.3x |

**The pooled percentile axis is a variable-composition mixture, not a common
axis.** Full per-class table in `docs/PRODUCTION_SHAPE_DECISION.md` §2.

**Method caveat that affects any scheme:** `N_ch` is discrete, so equal-
population classes are unattainable — realised class sizes run ~53k–153k
against a nominal ~91k. **The paper's 11-class scheme inherits this.**

**(iv) B_c — pre-registration confirmed.** Beauty-hard share **97.38 % /
97.68 % / 95.64 %**. B_c is rare (2.7–7.3 x 10⁻⁴ per event) with a ~2.7x spread
across tunes, which is hadronisation, not shape.

**(iii) DEFERRED — the beauty statistics projection was NOT run.** It is the
number that decides whether Option A's class coarsening suffices, and **the
starved-class question is unanswerable without it.** Requirements written into
the memo §4. **Do not substitute an estimate.**

---

## 5. Item 7 — memo written, STOP-AND-ASK pending

`docs/PRODUCTION_SHAPE_DECISION.md`. Three options costed, B_c subsection,
recommendation blank. **The summary table has not yet been put to the owner —
that STOP-AND-ASK is outstanding.**

**One figure in the brief contradicted:** the "~10x beauty per CPU-hour" for a
split descends from the superseded ~10:1 mixture. The event-count bound is
`1/0.1354 = 7.4x`, and **per CPU-hour it is lower still**, because beauty events
carry ~2x the multiplicity and cost more each. **The CPU-weighted factor is not
measured** and is listed as not established.

**The load-bearing coupling, recorded because it is easy to miss:** **B4 is
upstream of this decision.** MB-anchored boundaries decouple the axis from the
production shape and make a split clean; pooled-hard percentiles do not survive
a split unchanged, so choosing the shape first would lock in a redefinition of
the observable.

---

## 6. Not started

**Item 4** — proposal amendments (a) eligibility gap, (b) contract transition,
(c) mechanism note (**requires verifying from B5's difference lists that no
`mesonCvector`/`mesonBvector` key differs across arms**), (d) `D_s*`/`B_s*`/`B_c*`
scope with incremental pair-count cost, (e) parked card text + the
`effective_card_sha256` checklist.

**Item 8** — blocker reconciliation. Draft then **STOP for approval**; item 6's
numbers now sit behind the M6 entry. **Do not promote any category fraction
lacking block SEM.**

---

## 7. Candidate list — found, out of scope, NOT fixed

1. **`REPO_AUDIT_c1bb0d9.md:587` is stale** — the README row it flags was
   already fixed. The audit is a point-in-time artifact, but a reader treating
   it as current will chase a closed finding.
2. **`docs/handoffs/HANDOFF_20260808_v11.md` is untracked** and was left so —
   `docs/handoffs/**` is do-not-touch. **It should be committed by someone with
   scope**, or four days of record sit outside git again.
3. **The 11-class boundary scheme is not read from the paper.** I derived equal-
   population percentiles. The occupancy *gradient* is robust (it follows from
   the 2.05x separation) but per-class numbers would shift. **Someone should
   locate the paper's actual boundaries** — `status_analysis_qq.C:217,230` and
   `improvedPlotting_THnSparse.C:1211-1214` compute percentiles and are the
   likely authority.
4. **B_c's ~2.7x tune spread** (267 / 647 / 734 per 10⁶) is unexplained and is
   a hadronisation observable nobody has looked at.
5. **`REPRODUCIBILITY.md` §5 and §6 could not be renumbered** despite the gap,
   because `Makefile:45` and two docs point at "section 6". Left as-is
   deliberately; if the Makefile is ever in scope, renumber together.

---

## 8. Cold-read self-review

1. **Item 5's result is unseen.** §3 is a prediction plus two supporting prior
   measurements, not the run's own output.
2. **Item 6 used 10 jobs per tune, not the full 292.** Ratios and means are
   stable at 10⁶ events; **the class-occupancy tails (c11) are the least
   robust**, though 41,000+ beauty events per top class is ample.
3. **I did not verify the `hard_channel` semantics beyond the process-code
   cross-check.** That check is strong (exact, both directions) but it confirms
   consistency between two producer-written fields, not that either matches
   PYTHIA's truth.
4. **The memo's Option C cost is a bound, not a measurement**, and I said so in
   two places rather than quoting 7.4x as a result.
5. **Nothing was run on Nikhef that writes.** Item 5 writes only into its own
   retained run directory under `/data/alice/ipardoza/`, which is scratch, not
   the checkout and not promoted data.
6. **`make check` was not run.** All edits are documentation; no test covers
   them. That is an assumption, not a verification.
7. **No category fraction was promoted anywhere** — the block-SEM rule held.
