# HANDOFF v42 — the guard gap is closed, M7 beauty is measured, and the v1 decay map is WRONG

**Read `HANDOFF_20260810_v41.md` first for the merge's state; v40 for depth.**
This is the delta.

**The headline is not a task result. `docs/MAP_V1_CONJUGATION_BUG.md` — the
committed v1 decay-parent map does not conjugate antiparticle decays, and the
published experiment-comparable table is wrong by 17.8 percentage points on its
largest number. Nothing has been changed; it needs an owner ruling.**

---

## 0. STATE, AS OF 2026-08-10 23:25 CEST

| | |
|---|---|
| **Local HEAD** | **`e43753c`** on `physics-focus`, tree clean |
| **Nikhef HEAD** | **`43e35be`**, attached, **unmoved**; 2 untracked `.sub` files, pre-existing |
| **THE MERGE** | **RUNNING**, PID `3675829`, 52 min elapsed, **0 `PROMOTED_MERGE`** — correct, see below |
| **Condor** | **0 jobs.** M7b converged; nothing of mine is queued |
| **Checkout pin** | **ARMED** — `.git/checkout_pin` present, hook `2340a88f…` |
| Suite | **29 / 29, verified this session** — v41's inherited claim is now checked |

**The merge is in its internal-gate phase.** `merge_root_files.sh:80-83` re-runs
the full 3000-directory validation (~18.8 h) before merging anything. **A log
with no block lines is expected until ~17:30 CEST 2026-08-11.** Completion
window ~Aug 13 15:30 – Aug 14 03:30. If the log is still empty after ~19:30 on
Aug 11, that is past prediction and worth investigating.

---

## 1. ⛔ THE FINDING — READ THIS BEFORE TOUCHING THE MAP OR ITS TABLE

**`docs/MAP_V1_CONJUGATION_BUG.md` has the full case.** In one screen:

PYTHIA stores one decay table per particle. `particleDataEntryPtr(-413)` returns
the **+413** entry, so the probe reads **unconjugated** products for every
negative id, and `decay_parent_map_v1.json` carries them verbatim:

```
-413  D*-     dominant_products = [421, 211]    should be [-421, -211]
```

**D\*⁻ and D̄\*⁰ are therefore mapped to D⁰ instead of D̄⁰.**

**Proof is arithmetic, not inference:** published D⁰ − D̄⁰ = 46,379,976; the four
D\* states sum to 46,362,600; residual 0.04 %. **The whole published charge
asymmetry is the D\* states landing in the wrong bin.** Applying conjugation
sends D⁰/D̄⁰ from **4.49 → 1.00**, D_s⁺/D_s⁻ **2.74 → 1.00**, B⁰/B̄⁰ **5.39 →
1.00**. Charge symmetry is not restored across three families by accident.

**D⁰ moves from 45.95 % to 28.13 % — 17.8 percentage points.**

**Unaffected, checked rather than assumed:** the diquark-structure convention
(never consults the map), the **12.84 % second-branch number and its 97.81 % D\*
concentration** (computed from BR *fractions*, not daughter identity), M7 charm,
M7 beauty, the gate, the merge, the closure counts, and the ordinal table.

**Why nothing was changed:** the owner's check **C5** (v2 dominant-only must
reproduce the v1 table exactly) is now **mutually exclusive with correctness** —
passing it would require reproducing the defect deliberately. That is a ruling,
not an implementation detail. **`tools/build_decay_parent_map_v2.py` fail-closes
with `FAIL-CLOSED C2` and writes no artifact.** Three options with costs are in
§5 of the finding; **my recommendation is to fix conjugation as its own
reviewable change first, then build the BR-split on top**, so C5 survives as a
real check against a fixed v1.

> **How it survived:** the reader's invariance check asserts total weight is
> conserved, and conjugation errors move weight *between* bins while conserving
> the total exactly. My predecessor's re-derivation check reproduced the table
> **exactly and correctly** — both implementations were faithfully consuming the
> same wrong artifact. **A reimplementation check proves agreement, not
> correctness.** The only thing that could catch this was a check on the
> physics, and C2 is one. The owner specified it.

---

## 2. TASK 1 — THE GUARD GAP IS CLOSED, MECHANISED

**The hook shared the gap.** `.git/hooks/reference-transaction` built its
evidence from `condor_q` exactly as `make can-advance` does, so with M7b
converged **both** reported ALLOWED while the merge reads the tree. Confirmed
live before fixing: `CHECKOUT_ADVANCE_ALLOWED queue verified empty`.

**Fixed hook-side only**, as authorized. A pinfile check runs **before** the
queue probe, is **existence-based and fail-closed**, and **does not probe
liveness** — a liveness check can run on the wrong node or hit PID reuse and
degrade to ALLOWED, which is the failure being closed. **There is deliberately
no env-var override**: the control is the file, and removing it leaves a trace.

| | |
|---|---|
| pinfile | `/data/alice/ipardoza/Hadronization/.git/checkout_pin` |
| hook sha256 | `2340a88f8fab9d5b20bc1902481a9132768740dc42c104ea1bbb24efb5aa074a` |
| original backed up | `/data/alice/ipardoza/guard_hook_src/reference-transaction.ORIGINAL-526de52e` |

**Tested on a fresh scratch repo — no contact with the frozen checkout — in all
three directions**, with the queue *empty* (the hard case, since the probe would
have said ALLOWED):

- pinfile absent → **PERMITTED** ✓
- pinfile present → **REFUSED, ref unchanged** ✓ (while the log simultaneously
  recorded "schedd answered and holds nothing")
- pinfile removed → **PERMITTED again** ✓

**Then verified on the real checkout** by piping a simulated transaction into
the hook — inert, git moves nothing: `EXIT_CODE=1`, `state=PINFILE` logged, HEAD
unchanged.

> **⚠ TWO THINGS THE NEXT SESSION MUST KNOW.**
> 1. **`tools/install_checkout_guard_hook.sh` does not know about this edit.**
>    Re-running the installer **silently reverts the fix.** The hook header now
>    says so in a banner. The in-tree fix is authorized **post-freeze only**.
> 2. **Remove the pinfile only after verifying 33 `PROMOTED_MERGE` lines AND
>    that PID 3675829 has exited.** The date inside it is a margin, **not an
>    expiry** — if the date passes and the merge still runs, the merge wins.

---

## 3. TASK 2 — M7 BEAUTY, MEASURED

**`docs/M7_BEAUTY_UNRESOLVED_SYSTEMATIC.md`.** Cluster `5425788`, 10/10 blocks
`rc=0`, 30 `XCHECK_OK`, zero failures, 300 files per block, macro shas identical
across all ten logs.

| tune | unres rate % | baryon % | **relative shift %** | enrichment |
|---|---|---|---|---|
| MONASH | 0.0115 ± 0.0003 | 4.8715 ± 0.0037 | **0.0141 ± 0.0011** | 2.23× |
| JUNCTIONS | 0.1023 ± 0.0011 | 32.0174 ± 0.0115 | **0.0140 ± 0.0008** | 1.14× |
| CLOSEPACKING | 0.0983 ± 0.0011 | 32.3720 ± 0.0068 | **0.0143 ± 0.0007** | 1.15× |

**B1 HIT** (rates differ 8.9×), **B3 HIT**, **B4 HIT**, **B2 MISS — the shift is
flat**, all three inside one SEM. Reported as a miss, not rationalised.

**Why:** `relative_shift = rate × (enrichment − 1)` is an **exact identity** and
reproduces all six published values, charm and beauty. B2 followed from B1 only
if enrichment is tune-independent, which is what *charm* does (1.45–1.53).
Beauty's MONASH enrichment excess is **9.00×** JUNCTIONS' while its rate is
**8.90×** smaller — inverse to within 1 %, so the product is flat. **Whether
that reciprocity is mechanism or coincidence is a physics question and is left
to the owner; n=3 tunes is not a law.**

**The useful consequence:** because it does not vary by tune, the beauty
unresolved systematic **cannot bias a tune comparison** — a stronger statement
than "it is small".

**The owner's MONASH-zero contingency did NOT fire.** MONASH beauty unresolved
is **3,170**, not 0. The zero came from the single pre-flight file (~3.2/file);
**an n=1 reading nearly became an assumption about the measurement.**

---

## 4. TASK 3 — BLOCKED AT §1, BUT THE GROUNDWORK IS DONE AND VERIFIED

**Pre-registration `docs/MAP_V2_PREREGISTRATION.md` was committed before the
probe existed, and its prediction was confirmed exactly:**

| registered | outcome |
|---|---|
| D\*0 and D\*_s evaporate (all channels converge on one ground state) | **CONFIRMED** — D\*0: 0.619+0.381 both → D⁰; D\*_s: 0.942+0.058 both → D_s |
| D\*± survive as a genuine ~2/3 : 1/3 split | **CONFIRMED** — 0.677 → D⁰, 0.307+0.016 = 0.323 → D⁺ |
| exactly 2 species need splitting at 0.1 % | **CONFIRMED** |

**A discrepancy with the task brief, measured and reported:** the brief scoped
Task 3 as *"local-repo, analysis-side"*, and **it cannot be done that way** —
v1 and the retained probe output are strictly dominant-only, 202 species, zero
subdominant products anywhere. Resolved with an **extended probe** built and run
in Nikhef **scratch** (`/data/alice/ipardoza/f4b_runs/`), reading the frozen tree
and writing only to scratch. **Check C7 passed: the `F4_SPECIES` and `F4_GATE`
lines are byte-identical to v1's**, so the probe was extended, not changed.
**5,576 channel lines now exist** where 202 dominant channels did.

**A scope decision worth keeping.** Species-level aggregation is applied **only
to species v1 reassigns**, never to terminals. **B⁰ carries 890 channels whose
BRs sum to 1.32 and whose dominant channel is quark-level** — which is exactly
why v1 terminates it and treats it as an observable. Aggregating its channels
would move beauty weight into charm bins: a **convention change**, not the split
that was ruled for. Of the 60 reassigned species, **58 are clean** (BRs sum to
exactly 1, every channel has an in-table daughter); only `B_c±` are irregular,
and at **0.0031 %** they are far below threshold and stay dominant-only.

**Open for the owner (unchanged from the pre-registration):** BR provenance.
v2 uses **PYTHIA 8.317**, matching v1 and matching the generator that produced
the weights. **The brief asked for PDG-pinned values**; those are different
numbers and I have no offline PDG edition to pin. Not decided unilaterally.

---

## 5. WHAT I DID NOT DO

- **Task 5 (doc hygiene) — NOT STARTED.** All three items remain: the dated
  annotation atop `MERGE_V3_PREREGISTRATION.md` pointing at §5's withdrawal; the
  `--report` clarification beside the prohibition list; and confirming the
  frontier max-index-not-count correction reached the frontier-probe method doc.
  **Small, self-contained, and the first thing to pick up.**
- **No corrected experiment-comparable table was adopted.** §3 of the finding
  has the numbers; adopting them is the owner's ruling.
- **`decay_parent_map_v2.json` does not exist.** The builder refuses.
- **Task 4** was passive throughout, as instructed; nothing to validate yet.

---

## 6. YOUR ORDERED QUEUE

1. **Put §1 to the owner.** It is the largest open item and it blocks Task 3.
   Options and a recommendation are in `docs/MAP_V1_CONJUGATION_BUG.md` §5.
2. **Task 5**, the three doc-hygiene items. Cheap, and each protects a cold
   reader from a trap that already caught someone.
3. **Merge watching**, counts only, long intervals. First block due ~17:30
   Aug 11; validate it against **2643 s ± 30 %** and **~836 MB** RSS, record
   hit/miss, **then stop projecting** — unit costs are ordered and every prefix
   lies pessimistically.
4. **On the owner's ruling**, finish Task 3: fix conjugation, rebuild v2,
   regenerate the table, run C1–C7, confirm the residual lands under ~1 %.
5. **Not before the merge lands:** closure at scale under the **inverted** v3
   table — **2100 content / 1500 invariant**; 1800/600 means **FAILURE**.

**If the merge dies:** re-run `merge_runs/merge_v3_launch.sh` verbatim. Resumable
by validation, refuses to overwrite, **never delete partial outputs**, and every
resume re-pays the ~19 h internal gate. **Leave the pinfile in place across a
resume.**

---

## 7. COLD-READ SELF-REVIEW

Traps a cold reader would hit, now fixed above: that an empty merge log is the
internal gate and not a stall (§0); that the pinfile's date is a margin and not
an expiry (§2); that re-running the guard installer silently reverts the fix
(§2); that Task 3's "local-repo only" scoping is impossible (§4); and that the
12.84 % number **survives** the map bug, which is not obvious given they concern
the same artifact (§1).

**What I could not resolve:** I did not see a single `PROMOTED_MERGE` line, so
everything about the merge past its shape check remains prediction — inherited
from v41 and still unverified. And **I found the conjugation bug but did not fix
it**; the corrected numbers in §3 of the finding are a *measurement of what the
table would become*, not an adopted result.

**One thing I got wrong this session:** my first hook test scripted `rm -rf` on
the directory I had just copied the hook into, so **no hook was installed** and
the test still printed "CORRECT" twice. The `cp` error in the output was the only
evidence. Caught, fixed with a fail-fast install check, re-run. **A test that
cannot fail is not a test** — and this one had been about to certify a safety
mechanism.
