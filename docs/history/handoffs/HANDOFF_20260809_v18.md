# Handoff v18 — B4 criterion FAILS (escalation), stage-1 started, campaign healthy

**Date:** 2026-08-09. **Branch:** `physics-focus`.
**HEAD at time of writing:** `43b9d31` (local).
**Nikhef HEAD:** `e6429b7` — **FROZEN, unmoved.**

> ### CHECKOUT FREEZE STILL IN FORCE — HF_RUN3_V1 IN FLIGHT
> ~2829 of 3000 jobs have not started; they verify their commit at startup.
> **The checkout does not move until the campaign completes.**

---

## 1. THE HEADLINE: B4's pre-registered criterion FAILED — owner escalation

> **5 of 11 boundaries outside ±3 pp. The MB-anchored convention is NOT
> ratified. No convention change was made, nothing was recorded as ratified in
> M6/B4, and the boundary-grade MB follow-up was NOT started** — exactly as the
> pre-registration specified.

Full 33-entry table in `docs/PRODUCTION_SHAPE_DECISION.md` §5b. Failures:

| class | spread (pp) |
|---|---|
| c3 | **6.24** |
| c4 | **6.92** |
| c5 | **4.18** |
| c6 | **3.87** |
| c8 | **3.38** |

Passing: c1 0.00, c2 0.20, c7 2.54, c9 2.53, c10 1.93, c11 1.08.

### The difference check ran first and passed — so this is a real result

| tune | MB ⟨N_ch⟩ | hard ⟨N_ch⟩ |
|---|---|---|
| MONASH | 12.9482 ± 0.0290 | 11.2041 ± 0.0218 |
| JUNCTIONS | 13.5746 ± 0.0300 | 12.3250 ± 0.0232 |
| CLOSEPACKING | 12.9152 ± 0.0283 | 11.5931 ± 0.0218 |

Every pair separates decisively on at least one arm (MB/hard σ): M↔J 15.0/35.2,
M↔C **0.8**/12.6, J↔C 16.0/23.0. All three read distinct cards
(`B4_TUNE_CARD`). **The `Tune:pp = 14` failure mode is excluded.**

**Flagged, not glossed: MONASH↔CLOSEPACKING MB is only 0.8σ**, so the literal
MB-only check is marginal for that pair. **Physics, not plumbing** — `pT0Ref`
2.28 vs 2.194 are close, JUNCTIONS at 2.15 is the outlier, and the MB means
order accordingly.

### Structural reading — offered as context, NOT a resolution

Failures concentrate in **low-to-middle classes; the extremes agree under
2 pp**. **N_ch discreteness is the visible driver:** at c3 one integer of
boundary movement is ~6 pp of MB population; at c11 the same step is ~1 pp.
**That would make ±3 pp possibly unachievable in principle for low classes on a
discrete axis — a different problem from "the tunes disagree".** Which it is,
is the owner's call, and deliberately not made here.

**Six runs, 200k events each, all exit 0, overflow 0. Artifacts retained at
`/data/alice/ipardoza/b4_mapping/`; deployed macro `884f76e`, sha256
`3be7a09…`.**

---

## 2. Campaign — healthy, and one verification still unreachable

| | |
|---|---|
| Queue | **2829 idle, 82 running, 0 HELD** (read by reason) |
| Promoted | **MONASH 89**, JUNCTIONS 0, CLOSEPACKING 0 |
| Validator failures | none |

**~171 of 3000, still inside MONASH's block** (jobs 0–999).

**TUNE-INDEX SEED CHECK STILL OUTSTANDING.** JUNCTIONS starts at job 1000,
CLOSEPACKING at 2000. **MONASH cannot exercise the tune term of the v2
formula.** On first sighting: **JUNCTIONS job 0 ⇒ `131000001`**,
**CLOSEPACKING job 0 ⇒ `132000001`**.

**Retry round not earned** — zero holds. Recorded *why* the absence is expected
rather than merely observed: the guard fires above 3600 s CPU, MONASH's mean is
~377 s, and **the CR tunes carry essentially the whole hang history** (means
659 s / 989 s), so holds should be expected once jobs 1000+ start.
**`resubmit_held.py` remains untested on a batch.**

---

## 3. Stage-1 — two of five parts done

**Fixture (`bec170f`):** `HF_PT2_INT/raw/JUNCTIONS/hf_JUNCTIONS_job001.root`,
sha256 `49657c2c9a25e319513be5cda659a4d5e53bb3944f33bef51702b5660aaa3651`,
96,578,417 B, **verified byte-identical after transfer**, in local scratch.

**Species ordinal generator (`bec170f`):** `tools/GenerateSpeciesOrdinals.C`,
derived from the hashed `heavy_stability_audit` rather than hand-written.

> **CORRECTION TO THE BRIEF: the table is 202 species, ordinals 0..201 — not
> 0..218.** 219 is the total audit row count; **17 are hidden-heavy** (J/ψ, Υ,
> χ) with `q_c = q_b = 0`. They carry no net heavy flavour and cannot
> compensate, so they do not belong on a compensation axis — including them
> would put 17 bins on the axis that can never be filled by construction. They
> are counted and reported, not silently dropped. **219 − 17 = 202.**

Cross-check against the independent earlier measurement: **in_registry=50,
outside_registry=152** — both match exactly.

Two guards built in: **F6 fail-closed** stated in the artifact
(`unmapped_policy: fail_closed_no_overflow_bin`), and the generator **refuses
to emit** if any row has `final_may_decay != 0`, since the whole construction
assumes stability and a non-partition axis is meaningless.

**F5 supersession policy drafted (`eb73307`)** into the proposal for owner
review — schema-tagged directories, a `relation_to_prior` field with three
values where a consumer finding two views and no field must **fail** rather
than guess, nothing deleted, and **stage-1's own case is `coexists_with`, not
`supersedes`**, because it changes no physics and Measurements 1–3 stand.
Three open questions left undecided. **No output-layout code written.**

**NOT DONE:** the `hFlavourClosure` species axis implementation and its local
validation (closure sum rule to 1e-9; species-axis totals reproducing the
6-category axis bin-for-bin). Context.

---

## 4. Cold-read self-review

1. **The B4 escalation is the session's main output and I did not act on it
   beyond reporting.** No convention change, no M6/B4 edit claiming
   ratification, no follow-up production. That is the pre-registration, and it
   was written before the numbers existed.
2. **The discreteness reading is mine and is not evidence.** It explains the
   pattern plausibly; it has not been tested, and I did not let it soften the
   verdict.
3. **MONASH↔CLOSEPACKING MB at 0.8σ is the weakest link in the difference
   check.** I judged the guard satisfied on the strength of the hard arms and
   the distinct cards. **A reader who disagrees with that judgement should
   treat the whole harvest as unconfirmed** — the call is visible rather than
   buried.
4. **The species table's 202 vs the brief's 219 is a correction I made
   unilaterally.** The reasoning is in the commit; if hidden-heavy states are
   wanted on the axis for some purpose I have not anticipated, the generator
   needs one line changed.
5. **`pgrep -f run_b4` matched my own ssh command lines** while checking
   liveness — the pattern-match hazard from v7 §8, harmless here because
   everything was read-only, but it is still the wrong idiom and I used it.
6. **Nothing was written to the Nikhef checkout or promoted data.** All new work
   went to `b4_mapping/` scratch or local.
