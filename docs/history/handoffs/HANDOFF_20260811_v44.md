# HANDOFF v44 — band validated, advisory diagnosed, guard closed, anchor committed

**Read `HANDOFF_20260811_v43.md` for the maps; v42 for the conjugation finding;
`docs/ERROR_RECORD.md` for the error culture.** This is the delta.

**All four briefed tasks are complete. The merge did not finish — 4 of 33 — so
the freeze stands, the pinfile stays, and closure at scale was correctly not
started.**

---

## 0. STATE, AS OF 2026-08-11 19:08 CEST

| | |
|---|---|
| **Local HEAD** | **`84d6623`** on `physics-focus`, tree clean |
| **Nikhef HEAD** | **`43e35be`**, attached, **unmoved** |
| **THE MERGE** | **RUNNING**, PID `3675829`, **20 h 36 m**, **4 / 33 `PROMOTED_MERGE`** |
| promoted | `complete_root_…_MONASH` (central), `combined_root_{1,2,3}` (MONASH blocks) |
| **Guard** | pinfile **PRESENT**; deployed hook **`2340a88f8fab9d5b`** — unchanged |
| Condor | 0 jobs | 
| Disk | 1.1 T free |
| Suite | **29 / 29**, run twice this session |

| artifact | sha256 |
|---|---|
| `decay_parent_map_v1_1.json` | `dd502a10c5932fff…` |
| `decay_parent_map_v2.json` | `c9593c9c0a7c4ec2…` |
| `anchors/extraction_dual/per_species.csv` | `6137f6bc1f661ffd…` |

**The merge is ~12 % through by count and cannot finish soon. Do not remove the
pinfile.** Removal needs **33 `PROMOTED_MERGE` AND PID 3675829 exited** — both.

---

## 1. TASK 1 — BAND VALIDATED: **M1 HIT** ✅

`docs/MERGE_V3_BAND_VALIDATION.md`. Three full block cycles inside the
1850–3436 s band; **mean 2724 s vs the 2643 s anchor, 3.1 % high.**

> **The apparent outlier was a measurement artefact, and finding that out was
> the work.** `merge_one()` runs *merge → validate(.partial) → promote*, and
> validation only **reads** the partial — so **a directory's mtime marks the end
> of its MERGE**, not its promotion. Differencing consecutive mtimes measures
> `validate(N-1) + merge(N)`, which puts **the central's validation inside the
> block-1 interval** — on an 11 G output against a block's 1.8 G.
>
> It closes quantitatively: block 4's merge leg is **directly** measured at
> **1606 s**, a block's validation is **~739 s**, and the implied central
> validation of **~4500 s is 6.1× a block's against a 6.1× larger output**. A
> time ratio matching the size ratio is what settles it. **Block 1's merge was
> ~1870 s — in band.**

**RSS below band** (merge child 516 MB, validate child 570 MB vs 836 MB anchor):
recorded, not investigated, per the owner's asymmetric rule. **n=2, so no band
revision proposed.**

Also scored: **the internal gate finished 10:53:26** against v43's ~11:10 from a
single frontier reading — **17 min early on a 12 h prediction**, and **2.25×
faster than the standalone gate**. The central took **4.07 h** against a
registered ~7.3 h that was labelled an extrapolation when written.

**No completion projection is offered**, per the ordered-unit-cost rule. 4/33 is
the report.

---

## 2. TASK 2 — b-BARYON ADVISORY: raw, but it **leans machinery** ⚠

`docs/B_BARYON_ADVISORY_DIAGNOSTIC.md`. Ladder run in order, then stopped.

**Step 1 — the map is exonerated.** The asymmetry is in the raw weights with no
map applied. **It also explains the advisory's shape: raw Λ_b⁰ is 1.007 —
symmetric.** The 1.111 the advisory reported is *inherited*, because the map
chains the strongly asymmetric Σ_b/Σ*_b **into** the Λ_b bin, exactly as
designed. **The advisory pointed at a real feature of the input through a
correct mapping.**

**Step 2 — BLOCKED, not skipped.** The anchor is MONASH-only; the CR-vs-MONASH
test needs the merged three-tune output. **Deferred to post-merge, where it is
cheap.** What can be said: **the asymmetry appears in MONASH, which has no
colour reconnection and no junctions**, so junction transport is not required to
produce it.

**Step 3 — far out of line ⇒ machinery implicated, so it does NOT park.**

| group | ratio | asymmetry | σ |
|---|---|---|---|
| ground Λ_b⁰ | 1.007 | 0.37 % | 2.1 |
| ground Λ_c⁺ | 1.011 | 0.57 % | 9.2 |
| **excited Σ_b (spin-½)** | **1.468–1.671** | **19–25 %** | **31–41** |
| excited Σ*_b (spin-3/2) | 1.096–1.188 | 4.6–8.6 % | 10–18 |

**Σ_b at 25 % against Λ_b at 0.37 % is ~68× out of line at 41 σ — not
statistics.**

> **The structure is the finding: it sorts by LIGHT-DIQUARK SPIN**, not by
> excitation or heavy flavour. Spin-0 diquark states (Λ_b, Ξ_b) sit at
> 0.4–3.8 %; spin-1 diquark states (Σ_b, Ξ'_b, Ω_b, Σ*_b) at 4.6–54 %. **This
> project's primary convention is a diquark-structure grouping**, so a
> diquark-correlated effect is exactly what would matter — and exactly what
> could be either real fragmentation behaviour or a counting error.

**Constraint on all of it:** these are **extraction weights** — the compensation
observable's decomposition — **not inclusive yields**. The asymmetry could be
selection-side, and **a spin-dependent pattern is what selection-side would look
like**. Not assumed either way. Mesons are symmetric in both sectors; only
baryons are not; aggregates show no gross conservation violation.

**Not escalated as a bug** — a flagged pattern is not evidence of an error. The
affected weight is **~0.06 % of total** and cannot move a published number.
**Two cheapest discriminators, both post-merge:** step 2 across three tunes, and
a comparison against inclusive yields.

---

## 3. TASK 3 — GUARD + INSTALLER CLOSED (local only) ✅

`tools/checkout_advance_guard.py` and `tools/install_checkout_guard_hook.sh`.

The hook has carried a pinfile check since Aug 10, but it was **edited in place**
and the installer still emitted the old body — so re-running the installer would
silently revert it, a hazard the last two briefs carried in prose. **Both sides
now know about pinfiles.**

**The pinfile outranks `--override-reason`**, deliberately: the hook is the
actual enforcement and has no override, so a guard answering OVERRIDE would
contradict it. **The guard's job is to predict the hook, not to outrank it.**

Verified with a test that **confirms it is testing the patched objects before
asserting**: 5/5 guard unit cases including pin-beats-override, then the hook
body **extracted from the installer** and exercised in a scratch repo — not
PINFILE without a pinfile, PINFILE with one (echoing the pin), and **reverting on
removal**, which is what proves the new block fired.

> **NOT DEPLOYED, by instruction.** The running hook is still the in-place copy
> `2340a88f`. **Deployment post-freeze is just a re-run of the installer** — and
> after this change that re-run is safe.

---

## 4. TASK 4 / REVIEW READINESS — the anchor is now in the repo ✅

**`AnalysisScripts/anchors/extraction_dual/` + `MANIFEST.md`.** Until today, the
three CSVs behind **every number** in `EXTRACTION_CONVENTIONS`,
`SECOND_BRANCH_WEIGHT`, `MAP_V2_RESULT` and the b-baryon diagnostic existed
**only as uncommitted files in Nikhef scratch** — no published number was
reproducible from the repository. 4.5 kB; verified to reproduce the published
tables from the committed copies.

**The manifest does not manufacture provenance.** The source directory held the
three CSVs and **nothing else** — no log, no manifest, no invocation record — so
tune, directory count, reader commit and date are **unrecoverable**. The only
claim is prose, and **the prose disagrees with itself**: `EXTRACTION_CONVENTIONS`
§6 says *one directory*, v40 §6 says *four*. **Recorded as unverified.**

### ⚠ Still only in handoff prose — promote before review

1. **The anchor's tune and input count** (§4 above). Unresolvable without
   re-running; the manifest says so plainly. **The post-merge extraction must
   write an invocation manifest beside its outputs.**
2. **Merge timings** live in `docs/MERGE_V3_BAND_VALIDATION.md` now, but the
   **merge log itself carries no timestamps and no RSS lines** — every timing is
   reconstructed from filesystem mtimes. A reviewer cannot re-derive them once
   scratch is cleared. **Consider committing the merge log with the run
   manifest at completion.**
3. **`m7b_runs/`, `f4b_runs/`, `gate_runs/` outputs** are scratch-only. M7
   beauty's numbers are in `docs/`, but the ten block logs behind them are not
   committed.

---

## 5. WHAT I DID NOT DO

- **Closure at scale — correctly not started.** The bright line required the
  merge *harvested*; it is 4/33. When it runs: **2100 content / 1500 invariant**;
  **1800/600 means FAILURE on v3.**
- **Species decomposition / block SEMs** — next generation, and **it must run
  against v2**, not v1.
- **No deployment** of the guard fix; **no pinfile removal**; **no `Paper/**`
  edits** (checked: it contains no occurrence of the superseded numbers).

---

## 6. YOUR ORDERED QUEUE

1. **Watch the merge by counts, long intervals.** Band already validated — **do
   not re-validate, and do not project.** Resume = re-run
   `merge_runs/merge_v3_launch.sh` verbatim; never delete partials; leave the
   pinfile in place across a resume.
2. **On completion** (33 AND PID exited, both verified by count): verify shape
   3 centrals @1000 + 30 blocks @100, **score the wall clock against 65–77 h**,
   then remove the pinfile, then **deploy the guard fix by re-running the
   installer** (safe now).
3. **Then closure at scale** under the inverted v3 table, pre-registration first.
4. **Step 2 of the b-baryon ladder** once three tunes exist — cheap, and the
   discriminating test.
5. **Review readiness**: the three §4 items.

---

## 7. COLD-READ SELF-REVIEW

Traps handled above: that block 1 looks 2.4× over band and is not (§1); that the
advisory's Λ_b 1.111 is inherited rather than intrinsic, and raw Λ_b is
symmetric (§2); that the guard fix is **committed but not deployed** (§3); and
that the anchor's tune is **inherited prose, not verified** (§4).

**What I could not resolve:** step 2 of the ladder is genuinely blocked on
three-tune data, so the b-baryon question is **surfaced and characterised, not
answered** — I can say the pattern sorts by diquark spin and is 41 σ, not
whether it is physics. **validate(central) is inferred**, not measured; the 6.1×
size agreement is corroboration, not proof. And I never saw a merge run to
completion, so the 65–77 h band remains **unscored** three generations after it
was registered.
