# Handoff v14 — blocker reconciliation, proposal restage, G1 code changes

**Date:** 2026-08-09. **Branch:** `physics-focus`.
**HEAD at time of writing:** `f44b3c1`.
**Nikhef HEAD:** `e690e17` — **STILL FROZEN. The sync did not run this session.**

**This header is load-bearing again from the next session on:** the sync is the
next action, and after it the two HEADs must be compared, not assumed.

---

## 1. Per-item status

| item | status | commit |
|---|---|---|
| 1 — blocker reconciliation | **landed** | `8e67373` |
| 2 — proposal restage | **landed** | `78e3e13` |
| 3 — F1 verify + cost | **STOP-AND-ASK presented, decision NOT recorded** | — |
| 4 — B2 | **landed** | `81a350c` |
| 4 — B9 | **item-STOP — already fixed** (`e403853`) | — |
| 4 — B13 | **landed** | `f44b3c1` |
| 4 — guard demotion | **item-STOP — DEFINITION DOES NOT EXIST** | — |
| 5 — Nikhef sync | **NOT STARTED** | — |
| 6 — B6 rung-6 targets | **NOT STARTED** | — |
| 7 — submit-readiness walk | **NOT STARTED** | — |

`03123a2` → `f44b3c1`. **Local suite 24/24 green after every code commit.**

---

## 2. THE GUARD DEMOTION HAS NO DEFINITION — item-STOP, needs the owner

**Every reference is a bare list mention with no antecedent:**
`HANDOFF_v7:281`, `v8:340`, `v9:155`, `v10:131`, `RELEASE_BLOCKERS.md:1143`,
`docs/PHASE4_SUBMISSION_CHECKLIST.md:168`, and this session's `RELEASE_BLOCKERS.md:56`.
**Not one says which guard, what it is demoted from, or to what.**

The only substantive `demot*` hits in the tree are unrelated: B10's
blocker-half demotion, and carrier-uniqueness demotion in the origin-resolution
logic (`docs/DESIGN_AND_RATIONALE.md:177`,
`PUBLICATION_READY_CODING_AGENT_INSTRUCTIONS.md:937,944,2362`) — a physics
mechanism, not a pending change.

**The earliest mention (v7) already treats it as known**, so the definition was
verbal or lost before v7 was written.

**Not guessed at, because a guard change is the "weakens a validator" class.**
Three candidates, none acted on: the `MAX_CPU` hang guard (`Makefile:38-53`),
the `merge_one()` "final directory appeared before promotion" guard
(`merge_root_files.sh:178-181`), or the carrier-uniqueness demotion above.

**It is on the G1 list. Either the owner names it, or it is struck from G1 —
and striking it is a shrink, which the shrink-only rule permits.**

---

## 3. F1 — verified and costed, awaiting signature

**Leg (a) VERIFIED, and stronger than the brief stated.** `event_weight` is a
scalar `double` branch (`heavyflavourcorrelations_status.cpp:709`);
`sumWeights`/`sumWeights2` are scalar doubles (`:849-850`, `:1402-1403`,
`:1622-1623`). **`ValidateRawOutput.C:976` pins the schema explicitly as
`{"event_weight", "Double_t", 1}`** — a *declared contract* asserting exactly
one double per event — and `:466-467` reads the sums through `ReadScalar`.
**18 files touch the weight contract.** `UncertaintyBands` appears nowhere.

**Leg (b) HALF-VERIFIED, and the handoff says which half.** *Repo side,
verified:* no hadronisation-variation weighting exists anywhere. *PYTHIA side,
argued not tested:* the automated machinery reweights parton-shower emissions
and PDF members; hadronisation parameters act after the shower and change which
hadrons form, so there is no fixed configuration to reweight. **Supporting
evidence in-tree: the study's three arms already are hadronisation variations
handled by separate runs.**

**Cost of ON:** raw is **88.0 MiB/job** (27 GB at 300 jobs → ~264 GB at 3000);
+8 bytes x N weights per event ⇒ **~+18 % at N≈20 → ~312 GB**, both inside the
1.6 TB free. **CPU overhead ~10–30 %, ESTIMATE, not measured.**

**Decision note as presented:** ON costs ~18 files' contracts plus a declared
schema assertion; OFF means scale/PDF systematics are argued, not computed, in
the referee response (ties to **B14**); **recommendation OFF.**

**On GO:** record in `docs/DESIGN_AND_RATIONALE.md` and the F1 gate line.

---

## 4. What landed in the code

**B2 (`81a350c`).** All three submit targets now `--burn-seeds`.
**The prelim decision is made and commented in the Makefile: all three burn** —
a prelim run produces real data with real seeds, and there is no principled
reason for the 50-job target to skip when the 10- and 1000-job targets do not.
**The render-vs-submit docstring inconsistency is resolved, not deepened:**
render-time burning is correct (a rendered-but-unsubmitted `.sub` has reserved
its seeds, so a re-render cannot reuse them), so the docstring was wrong and now
states the contract and its cost. **No behaviour change in `campaign.py`; the
ledger is untouched.**

**B13 (`f44b3c1`).** Failure-path `rm -f` removed; success path byte-identical;
stderr line added naming the retained log. Verified by `git diff` showing a
single deletion, `bash -n`, and 24/24.

**B9 — already fixed.** `resubmit_held.py` derives via
`campaign_ordinal_on_disk()` (`:71-89`), `--campaign-ordinal` defaults to `None`
(`:99-100`), derivation with a multi-ordinal refusal at `:136-145`. Landed
`e403853`; `test_resubmit_held_ordinal.py` passes.

---

## 5. The sync — NOT STARTED, and the preconditions that are already checked

**`condor_q` verified empty for `ipardoza` by full output** (no `-af` shortcut):
`Total for ipardoza: 0 jobs`.

**Pre-registered expectations for the next session to test, unchanged:**

- **`make check` bare on Nikhef: the brief pre-registers 23/23.** **Locally it
  is 24/24.** The difference is `tests/test_resubmit_held_ordinal.py`, added
  with B9's fix (v7 recorded the 23→24 transition explicitly as "the changed
  count is this test, not a regression"). **So 24/24 is the expectation on a
  synced checkout; 23/23 would mean the sync did not carry B9's test.** Flagged
  because acting on the brief's 23/23 unexamined would misread a real failure
  as success.
- **Producer rebuild: byte-identical `e54b27bb9e3f…`.** Nothing in items 1–4
  touched the translation unit — the only code edits were `Makefile`,
  `tools/campaign.py` (docstring), and `Validation/validate_pair_block_closure.sh`.
  **If the SHA moves: SESSION-STOP.**

---

## 6. Candidate list — out of scope, NOT fixed

1. **`docs/audit/**` are point-in-time records, deliberately unedited.**
   `REPO_AUDIT_c1bb0d9.md:587`'s README row is already fixed; a reader treating
   the audit as current will chase closed findings. Carried from v12, v13.
2. **The memo's 11-class scheme is mine, not the paper's** — carried from v13.
   `status_analysis_qq.C:217,230` is the likely authority.
3. **B_c's ~2.7x tune spread** (267/647/734 per 10⁶) is unexplained.
4. **`REPRODUCIBILITY.md` §5/§6 cannot be renumbered** while `Makefile:45`
   points at "section 6".
5. **The 6(iii) run merged c10+c11** through a boundary-list error of mine;
   redo (~20 min, detached) only if those classes are quoted separately.
6. **The M1–M10 review is unarchived** — now recorded as an OWNER ACTION line in
   `RELEASE_BLOCKERS.md`, but the entries citing it still cite something a cold
   reader cannot open.

---

## 7. Cold-read self-review

1. **I did not start the sync**, deliberately: it carries a SESSION-STOP
   condition on the producer SHA, and beginning it near the context limit would
   have meant handling that stop badly. **The freeze holds; nothing is
   half-done on Nikhef.**
2. **B2's prelim decision is mine, not the owner's.** B2's text authorised
   deciding it, and the reasoning is in the Makefile comment — but it is a
   policy choice about an irreplaceable global resource and deserves a glance.
3. **The docstring fix changed no behaviour**, which is the claim that matters;
   `git diff` on `campaign.py` is docstring-only.
4. **I have not verified that `make check` on Nikhef will find its environment**
   — `tools/run_tests.sh`'s caveat applies and I have not read it this session.
5. **F1's leg (b) is argued, not tested**, and §3 says so rather than implying
   otherwise.
6. **No category fraction lacking block SEM was promoted anywhere.**
