# HANDOFF v43 — the map is fixed and split; the merge is ahead of schedule

**Read `HANDOFF_20260810_v42.md` for the conjugation finding and the guard fix;
v41 for the merge launch; v40 for depth.** This is the delta.

**Tasks 2, 3 and 4 are complete. Task 1 could not run — the first merge block is
still hours out.**

---

## 0. STATE, AS OF 2026-08-11 04:18 CEST

| | |
|---|---|
| **Local HEAD** | **`e3c6083`** on `physics-focus`, tree clean |
| **Nikhef HEAD** | **`43e35be`**, attached, **unmoved** |
| **THE MERGE** | **RUNNING**, PID `3675829`, **5 h 46 m** elapsed, **0 `PROMOTED_MERGE`**, 9 log lines |
| **Guard** | pinfile **PRESENT**; hook **`2340a88f8fab9d5b`** — unchanged, installer not re-run |
| **Condor** | 0 jobs |
| Suite | **29 / 29**, run twice this session (after each builder change) |

| artifact | sha256 |
|---|---|
| `decay_parent_map_v1_1.json` | **`dd502a10c5932fff…`** |
| `decay_parent_map_v2.json` | **`c9593c9c0a7c4ec2…`** |

---

## 1. ⏱ THE MERGE IS ~6 HOURS AHEAD OF THE BRIEFED SCHEDULE

**The brief and v42 both said the first block was due ~17:30 CEST Aug 11.
Measured, it is closer to ~11:10.**

Method — the **corrected** frontier probe (max index, `atime > job_start`):

```
MONASH        slot_999   (complete, 1000/1000)
JUNCTIONS     slot_257
CLOSEPACKING  not started
              => ~1258 / 3000 directories
```

Elapsed 20,745 s since 22:32:12. Net of the ~49 min pre-loop verification pass,
that is **~14.1 s/dir**, against the **22.28 s/dir** the standalone gate ran at.
Remaining 1742 directories ⇒ **internal gate completes ~11:10 CEST**, first
`PROMOTED_MERGE` shortly after.

**Caveats, stated rather than smoothed over:**

- **This is ONE frontier reading.** The standalone gate's frontier ETA held
  across five independent readings, which is why the method is trusted at all —
  but n=1 here. **Re-probe before relying on it.**
- **It is ~1.6× faster than the same tool was two days ago.** Page cache from
  the standalone gate having just walked these directories is a plausible
  explanation and **is not asserted** — I did not measure it.
- **This is the GATE phase, not `merge_one()`.** The ordered-unit-cost rule
  bans projecting *merge* blocks from a prefix; it does not ban a frontier
  position on a workload measured to be flat. **When blocks start, stop
  projecting.**

**Nothing about the completion window changes yet** (~Aug 13 16:00 – Aug 14
04:00): 65–77 h was measured end-to-end, and a faster gate only moves the start
of block work.

---

## 2. TASK 2 — MAP v1.1, THE CONJUGATION FIX ✅

`docs/MAP_V1_1_PREREGISTRATION.md` (committed before the builder changed),
artifact `decay_parent_map_v1_1.json`.

**The built diff matched the pre-registration exactly:**

| | predicted | built |
|---|---|---|
| artifact rows changed | 101 | **101** |
| table-affecting rows | 60 | **60** |
| particle rows changed | 0 | **0** |
| rows with any other field changed | 0 | **0** |

**The probe was NOT re-run.** `f4_probe.out` records what PYTHIA reports and is
correct as a record; conjugation is *interpretation* and lives in the builder.

**Positive check — genuine two-implementation agreement**, not a restatement:
the regenerated table reproduces the numbers computed last session by a separate
direct walk. **D⁰ 36,539,688 (28.1326 %)**, D̄⁰ 36,437,040, D_s⁺ 5,491,128,
total 129,883,844 conserved. **All three charge ratios move to unity**: 4.49 →
1.003, 2.74 → 1.001, 5.39 → 0.998.

**Two permanent mechanisms added** (`tools/build_decay_parent_map.py`):
**involution**, fail-closed on every future build — an antiparticle row must
store exactly the conjugation of its particle row, the check that would have
caught this the day v1 was built; and the **heavy-quark sign check on every
row**, which distinguishes a legitimate flavour change (b→c) from a sign flip.

**The 12.84 % survives, and is now re-verified rather than asserted** —
identical under v1.1 (12.8400 / 12.8451 / 35.7910, same contributors), because
it is computed from BR *fractions*, not daughter identity.

---

## 3. TASK 3 — MAP v2, THE SPLITS ✅ — misassignment falls to **0.0018 %**

`docs/MAP_V2_RESULT.md`. Built on v1.1; **the builder refuses a v1 base map**,
verified by attempting it.

| | value |
|---|---|
| channel-level bound (v1 history, **superseded as the quoted figure**) | 12.8451 % |
| species-level, before splits | **5.7737 %** |
| **species-level, after splits** | **0.0018 %** |

> **The larger effect is not the split.** 7.07 pp evaporate on species-level
> accounting alone — channels landing on the *same* ground state were never a
> misassignment. The two splits remove 5.77 pp more. **C6 passes by ~550×**; the
> threshold was never lowered. Only B_c± remains, at 0.0009 % each.

**Exactly two splits**, D\*+ and D\*− at **0.677 / 0.323**, with D\*−'s bins
**conjugated** — correct only because v1.1 landed first.

**All checks pass:** C1 (exact rational fractions sum to 1), C2/I2, C3 (totals),
**C4 measured not asserted** (structural totals 67,969,216 / 60,600,180 /
1,314,400 / 48 reproduce exactly from the category column, which never consults
the map), **C5 re-pointed to v1.1 and byte-identical**, C6, C7.

**BR source ruling applied:** PYTHIA 8.317's own probed table, version-pinned,
**not PDG** — recorded in the artifact's `br_source` block.

### D⁰ across the three maps

| map | D⁰ | D̄⁰ | ratio |
|---|---|---|---|
| v1 (**defective**) | 45.9475 % | 10.2387 % | **4.49** |
| v1.1 | 28.1326 % | 28.0536 % | 1.003 |
| **v2 (current)** | **25.2425 %** | **25.1718 %** | 1.003 |

### ⚠ Open for the owner — the advisory's surviving flags

15 flags under v1 → **7** under v1.1/v2. The 4.49×/2.74×/5.39× are gone. What
remains is **flagged, not judged**:

**Λ_b⁰/Λ̄_b⁰ = 1.111**, Ξ_b ≈ 1.115, Ω_c⁰ = 1.169, Ω_b⁻ = 1.486. **Λ_c is 1.026,
consistent with the ~2 % the brief anticipated — but the b-baryons sit near 11 %,
five times that.** Whether baryon-number transport accounts for it is **a physics
question and the owner's.** The two "empty antiparticle bin" flags carry 48 and
26 weight; at those counts that is a statistics artefact.

---

## 4. TASK 4 — DOC HYGIENE AND THE ERROR RECORD ✅

- **`MERGE_V3_PREREGISTRATION.md`** now warns at the top that §5's bold
  MUST-NOT-LAUNCH is withdrawn eighty lines later. **Two sessions running came
  close to acting on the retracted version.**
- **`PHASE4_SUBMISSION_CHECKLIST.md`** now states the `--report` ban targets
  **standalone** invocation, that the merge's internal call is the designed
  path, that **resuming the merge is not a violation**, and that a silent log
  for ~19 h is the internal gate rather than a stall.
- **`GATE_3000.md`** now carries both frontier corrections *where the method is
  read*: compare against `job_start`, and **take the max index, never the
  count** — with the measured 934-vs-972 shortfall showing why.
- **Beauty-M7 framing needed no change**; it already leaves the reciprocity to
  the owner and says three tunes is not a law.
- **`docs/ERROR_RECORD.md` created** — the record moves out of the handoff chain
  where it was aging out of reach. **E1 is bidirectional**: the probe read
  PYTHIA verbatim for antiparticle parents (agent), and the 4.49× asymmetry
  passed review without a symmetry check being requested (owner). It also
  records the uncomfortable part — **the re-derivation check reproduced the
  table exactly and correctly, because two implementations of the same
  misreading agree perfectly.**

---

## 5. WHAT I GOT WRONG (E3, and it is in the record)

The v2 pre-registration scored **2 hit, 3 missed**, all three from one cause.
V3 (0.677/0.323) and V4 (exactly 2 splits) were exact. V1, V2 and V5 all erred
**in the safe direction** because, having established that D\*⁰'s channels
converge on one ground state, I **assumed the smaller species would not.**
Ξ*_c decays to π⁰ and γ on the same daughter — identical structure — and all
four states collapse to a single branch.

> **Having found a mechanism, check where else it applies before predicting.**

---

## 6. YOUR ORDERED QUEUE

1. **Re-probe the merge frontier immediately** (max index, `atime > job_start`).
   If §1 holds, the first block lands **~11:10, not 17:30**. **Validate it
   against 2643 s ± 30 % and ~836 MB RSS, record hit/miss, then counts-only at
   long intervals — and stop projecting the moment blocks begin.**
2. **Put §3's b-baryon asymmetries to the owner** (~11 % vs the ~2 % expected).
   Advisory by design; it is surfacing something, and only the owner can say
   whether it is physics.
3. **On merge completion** (33 `PROMOTED_MERGE` **AND** PID exited — both, and
   verified by count): remove the pinfile per the recorded protocol, then queue
   the **in-tree guard + installer fix** for a fresh session. **Do not start it
   late** — `tools/install_checkout_guard_hook.sh` silently reverts the hook.
4. **Not before the merge lands:** closure at scale under the **inverted** v3
   table — **2100 content / 1500 invariant**. **1800/600 means FAILURE**, not
   success.
5. Then blocks → block SEMs on the species decomposition, both conventions,
   three tunes — the number the resubmission turns on. **It should now be run
   against v2**, not v1.

**If the merge dies:** re-run `merge_runs/merge_v3_launch.sh` verbatim. Resumable
by validation, refuses to overwrite, **never delete partial outputs**, every
resume re-pays the internal gate. **Leave the pinfile in place across a resume.**

---

## 7. COLD-READ SELF-REVIEW

Traps a cold reader would hit, now handled: that the merge may be **hours ahead**
of the schedule in the brief (§1); that the v1 table is superseded and carries a
do-not-quote banner while the v1 numbers deliberately remain; that **C5 now
points at v1.1** and passing it against v1 would reproduce the defect; that the
`--report` ban and the merge only *look* like a conflict; and that the
particle/antiparticle report is **advisory on purpose** — a gate at 1.00 would
refuse real physics.

**What I could not resolve:** I never saw a `PROMOTED_MERGE` line, so every
merge timing here is still prediction, now two generations deep. **The 14.1 s/dir
rate is a single reading** and is 1.6× faster than the same tool ran two days
ago; I offer page cache as a plausible cause and did not measure it. And the
b-baryon asymmetries are **surfaced, not explained** — I can say they are five
times larger than anticipated, not whether they are real.
