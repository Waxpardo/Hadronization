# Handoff v13 — item-5 harvest, the 6(iii) decision number, proposal amendments

**Date:** 2026-08-09. **Branch:** `physics-focus`.
**HEAD at time of writing:** `5ed3bc9`. **Nikhef HEAD:** `e690e17` — frozen,
**read-only this session** (ROOT over ssh and file reads; scratch macros under
`/tmp` and retained run directories under `/data/alice/ipardoza/` only —
nothing written to the checkout, nothing submitted, checkout not moved).

---

## 1. Per-item status

| item | status | commit |
|---|---|---|
| 1 — commit stranded handoffs | **landed** | `f1ec6d8` |
| 2 — harvest item-5 RSS | **landed** | `a8853f2` |
| 3 — 6(iii) beauty projection | **landed** | `5ed3bc9` |
| 4 — proposal amendments (a)–(e) | **landed** | `a71b7d4` |
| 5 — blocker reconciliation | **DRAFT PRESENTED, awaiting approval — not written** | — |
| 6 — this handoff | — | — |

`bd811d0` → `5ed3bc9`. **No code, config, generated header, seed ledger, tune
card or `Paper/` content touched.**

---

## 2. THE DECISION NUMBER — 6(iii), and Option A passes

**Method:** per tune, per promoted block (10), per pair file: project
`hCorrelations` onto **axis 6 (N_ch)**, integrate within the realised class
boundaries from `bd811d0`, sum over each species group, **x10 for 1000 jobs**.
Exact entries, no occupancy proxy. Artifact
`/data/alice/ipardoza/proj6iii_run01/proj.out`, 150 lines.

**Mean entries per block at 1000 jobs, binding (lowest) class:**

| species | MONASH | JUNCTIONS | CLOSEPACKING | verdict |
|---|---|---|---|---|
| B-meson control | 1801 | 1467 | 1658 | pass |
| Λ_b | 156 | 167 | 217 | pass |
| **Σ_b^±** | **20** | 161 | 125 | **pass — binding value** |
| Σ_b⁰ *(not quoted)* | **7** | 75 | 74 | fails c1 |
| **B_c** | **0** | 1 | 0 | **fails c1–c7** |

> **VERDICT: Option A viable as-is for the quoted beauty observables.** Every
> quoted species (Λ_b, Σ_b^±, B-meson control) clears ≥10 entries/block in every
> class, all three tunes. **Binding value MONASH Σ_b^± c1 = 20**, clearing by 2x.

**Three qualifications, all in the memo:**

1. **MONASH's low classes are marginal, not comfortable** — Σ_b^± runs
   20/31/32/37 across c1–c5. **Per-class error bars will be large and must be
   shown, not smoothed.**
2. **Σ_b⁰ fails c1 (7).** Not quoted so it does not gate, but any completeness
   table including it inherits the gap.
3. **B_c per-class is not viable under ANY scheme** — fails c1–c7 at 11 classes,
   and coarse classes 1–2 at the 4-class variant. **B_c is a top-class-only
   observable in combined production. This is where the shape decision
   genuinely bites**, since a bb̄-only arm carries 95.6–97.7 % of B_c.

**Two defects in my own run, recorded in the memo:** the boundary list gave **10
intervals, not 11** (top bin merges c10+c11 — does not affect the starvation
verdict, which turns on low classes); and `min x 10` degenerates on an observed
zero, so **means are the decision statistic**.

**Pre-registration scorecard:** "low classes starved" — **too pessimistic**;
"MONASH Σ_b^± binding" — **confirmed**; "B_c thin everywhere" — **confirmed
strongly**.

---

## 3. Item 2 — pool RSS measured, sizing corrected

Run completed RC=0, artifact `/data/alice/ipardoza/poolrss_run01/time_v.txt`:

```
Maximum resident set size (kbytes): 584452       # 570.8 MiB
User 650.08 s  System 0.84 s  Elapsed 10:54.07  Percent of CPU 99%
```

**A single ROOT directory validator uses 570.8 MiB at 99 % CPU**, so 18.4 MB is
the Python orchestrator and cannot be a per-worker footprint. Both hypothesis
annotations (B10, checklist) are now **measured**.

**The pre-registered band was 400–460 MB; this came in ABOVE it**, so the
conclusion holds *a fortiori* — but **one detail of the pre-registered remedy
needed correcting.** Against the 2048 MiB login cap:

| | per worker | P=4 | P=8 |
|---|---|---|---|
| gate child | 440.9 MiB | 1764 MiB — fits | **3527 — EXCEEDS** |
| merge-side | 570.8 MiB | **2283 — EXCEEDS** | 4566 — exceeds |

**Corrected: P≤4 for the gate, P≤3 for merge-side — not P≤4 for both.** Prefer
a batch node (128 GiB cap).

---

## 4. Item 4 — amendments landed, one verification worth carrying

**(c) verified and it is stronger than stated:** `mesonCvector` /
`mesonBvector` appear **nowhere in the tree** — no card, no allowlist, no B5
difference list. All arms inherit the PYTHIA default. B5's
MONASH↔CLOSEPACKING list contains **no `StringFlav` key at all**.

**So the excluded fraction's tune-dependence flows through diquark/junction
parameters and the baryon fractions they produce.** The vector share still
differs across tunes (65.2 / 48.2 / 47.8 %) **at a fixed vector:pseudoscalar
ratio, because the baryon fraction differs and mesons are the remainder.**
State it that way or a reader hunts for a meson-vector parameter that does not
exist.

**(a) remains the sharpest open gap:** §0a and §4B of the proposal conflict.
Option B's excited associates cannot pass `EligibleBase` through the
ground-state registry, and §0a correctly forbids moving that registry.

---

## 5. Item 5 — blocker draft presented, NOT written

Four entries drafted and put to the owner in the session report: **M6**
(production-blocking), **production-shape decision** (open owner decision),
**excited-state gate** (pointing at the proposal's two design gaps), and
**submission-blocking** entries for the systematics gap and the review's
manuscript findings. **Awaiting approval before any file is edited.**

**No category fraction lacking block SEM appears in any draft entry.**

---

## 6. Candidate list — out of scope, NOT fixed

1. **`docs/audit/REPO_AUDIT_c1bb0d9.md:587`** — the README row it flags was
   already fixed. **`docs/audit/**` are point-in-time records and are
   deliberately left unedited**; a reader treating them as current will chase
   closed findings. Carried forward from v12.
2. **The memo's 11-class scheme is mine, not the paper's.** Equal-population
   percentiles; the occupancy *gradient* is robust but per-class numbers shift
   under other boundaries. `status_analysis_qq.C:217,230` and
   `improvedPlotting_THnSparse.C:1211-1214` are the likely authority.
3. **B_c's ~2.7x tune spread** (267/647/734 per 10⁶) is an unexplained
   hadronisation observable nobody has looked at.
4. **`REPRODUCIBILITY.md` §5/§6 cannot be renumbered** while `Makefile:45`
   points at "section 6". Renumber together if the Makefile is ever in scope.
5. **The 6(iii) run should be redone with 11 boundaries** if per-class c10/c11
   numbers are ever quoted separately. Cheap (~20 min, detached).

---

## 7. Cold-read self-review

1. **The 6(iii) verdict rests on means over 10 blocks**, which is the right
   statistic for "expected entries per block" but hides block-to-block spread.
   **MONASH Σ_b^± minimum over blocks was 0 in c1** — with mean 20, that is
   Poisson-consistent, but it means at least one block will be empty at that
   class even at 1000 jobs. **Anyone quoting per-block c1 Σ_b^± must handle
   empty blocks explicitly.**
2. **The x10 scaling to 1000 jobs assumes linearity in job count**, which is
   exact for independent events — but the blocks are 10-input subsets and I did
   not verify that a 1000-job campaign partitions into blocks the same way.
3. **I merged c10 and c11** through my own boundary-list error and reported it
   rather than re-running. If the top classes matter separately, redo it.
4. **`make check` was not run.** All edits are documentation.
5. **Nothing was written to the Nikhef checkout or to promoted data.** Scratch
   macros under `/tmp`, retained outputs under `/data/alice/ipardoza/`.
6. **No category fraction lacking block SEM was promoted anywhere.**
