# Handoff v33 — the gate is running; the merge is pre-registered and NOT launched

**Read v21 (`48cf3a3`) for generational context; v32 for the freeze's release.**

| | |
|---|---|
| **Local HEAD** | **`87bac5e`** on `physics-focus` |
| **Nikhef** | **`43e35be`** on `physics-focus` — frozen by in-flight jobs |
| **Heads match?** | **No.** Two jobs pin the checkout; guard and hook refuse. Correct |
| **The gate** | **RUNNING — cluster `5399423`** on `wn-sate-072`, ~21–33 h |
| **The merge** | **NOT LAUNCHED.** Conditional on gate PASS, which has not happened |
| 100-input anchor | **RUNNING — cluster `5399458`** on `wn-sate-061`, isolated |
| 10/25 re-run | **CLOSED UNRUN** — produced no data; see §3 |
| Suite | **29 / 29** |

---

## 1. ⚠ THE THING THAT MOST NEEDS A DECISION: the merge may not fit

**`merge_root_files.sh:80-83` runs `validate_analysis_outputs.py` itself**, to
`${analysis_root}/validation/analysis_output_manifest_validation.json` — a
*different path* from the standalone run's report. **So the gate is paid twice.**

| stage | expected |
|---|---|
| gate, inside the merge | 21–33 h |
| 33 merges (3 centrals @1000, 30 blocks @100) | 35–76 h |
| **total** | **56–109 h** |
| **site ceiling** (`MaxJobRetirementTime`) | **345600 s = 96 h** |

**The central estimate (~80 h) leaves ~16 h of margin on an extrapolation whose
every point is n=1. The pessimistic case does not fit at all.**

> **This is an owner decision.** The remedies — split the merge across
> invocations, or skip the internal gate — touch the production script, which is
> unmodified by standing rule. **Flagged before launch rather than discovered at
> hour 96.** The anchor (§3) is the cheapest thing that narrows the band.

This is B10's point (`RELEASE_BLOCKERS.md:742`) reaching its consequence: the
serial checksum gate is *on* the merge path, not merely near it.

---

## 2. THE GATE — first launch was my bug; second is running

### It died in 0.75 s, and only luck made that cheap

```
ValueError: canonical raw input is absent or stale:
  /data/alice/ipardoza/hadronization_production/raw/MONASH/hf_MONASH_job000.root
```

**`--production-root` was wrong.** `raw_path` in the manifest is
**campaign-relative**, and `hadronization_production/` holds one directory *per
campaign* (`HF_PT2`, `HF_PT2_INT`, `HF_RUN3_V1`, `HF_SMOKE`, `HF_SMOKE2`). I
passed the parent. Correct value: **`…/hadronization_production/HF_RUN3_V1`**.

**A 21 h job should not depend on which error surfaces first.** `run_gate.sh`
now preflights — resolves one row's raw file *and* receipt by the tool's own
rule, refuses before any directory is touched, exits 90.

**Relaunched as `5399423`. Preflight passed. Running.**

### Observability is thin — know this before you go looking

**The gate emits nothing until it finishes.** The log holds only the header
(358 bytes) and the report is written at the end. There is **no incremental
progress signal** — no "n of 3000". The only outcomes are:

- `gate_runs/GATE_3000_DONE` — contains `rc=…`, written whatever happens
- `gate_runs/gate_3000_report.json` — written on success
- `gate_runs/gate_3000.log` — header, then traceback or timing footer

**Do not read a quiet log as a stalled job.** Check `condor_q 5399423`.

### Pre-registration (`docs/GATE_3000.md`)

G1 38–48 s/directory, G2 32–40 h, G3 440–480 MiB, G4 `PASS directories=3000
missing=0`, G5 instant refusal on any `.partial.`. **G1/G2 were set at n=2 and
are expected to miss high** — the n=25 measurement says 25.43 s mean, ~21 h.
Recorded before the run, not revised.

---

## 3. THE SCALING CURVE — one point closed unrun, one point running

### The 10/25 re-run produced nothing, and is closed

`5399189` aborted on its own assertion:

```
ABORT checkout moved off the pin: 43e35be…
```

**A guard firing correctly on an expired premise.** It asserted `HEAD ==
61fe978f` — true during the freeze, meaningless after convergence lifted it. It
gated on the **checkout** when comparability needs the **program**, and the
program did not change:

```
MergeCanonicalAnalysis.C   61fe978 == 43e35be
MergeAnalysisObjects.C     61fe978 == 43e35be
git diff --stat 61fe978 43e35be -- AnalysisScripts/   → empty
```

The measurement would have been comparable. **Closed unrun per the owner's "no
third re-run".** The 10/25 points stand at their login-node values.

**`stage_MONASH_10_batch/` holds 33 pair files and an empty `t.txt`** — debris
from the run removed mid-flight last session. **Not a measurement.**

### The 100-input anchor — and isolation actually solved this time

`5399458`, **`wn-sate-061`**, while the gate runs on **`wn-sate-072`**. Submitted
with

```
Requirements = (Machine != "wn-sate-072.nikhef.nl")
```

**An explicit exclusion of my own heavy job's node** — the answer to a problem
the two previous attempts could only flag after the fact. It asserts the **macro
sha256** rather than the checkout, so it cannot fail the way `5399189` did.

**Why this point and not another:** `merge_root_files.sh:190-194` runs **30 block
merges at exactly 100 inputs** against 3 centrals. It is the merge's most-repeated
operation and the v3 curve stopped at 50.

#### Early rate — a direction, not a band

At 2.5 minutes in: 4 files, deltas **35, 34, 36 s**, mean **35.0 s/file** →
**10,500 s (2.9 h)** for 300.

**That is 2.3× above M1's upper bound of 4500 s — and it should NOT be read as
M1 missing.** The standing caveat (§6) applies exactly here: `n=3`, and the
50-input run behaved the same way, opening at 11.8–14.7 s/file and finishing at a
**5.31 s/file average** — a 2.2–2.8× acceleration. Apply that factor and the
anchor lands near **4200 s, at the top of M1's band.**

> **Recorded as a direction with its `n`, per the convention, rather than as a
> verdict.** If the anchor finishes near 10,500 s the acceleration did not happen
> at 100 inputs and **M1 misses high — which would also push M2, M5 and §1's
> ceiling problem materially worse.** That makes this the single most
> consequential number outstanding.

---

## 4. THE MERGE — fully pre-registered, deliberately not launched

`docs/MERGE_V3_PREREGISTRATION.md`. **GO is conditional on the gate's PASS**, and
the gate will outlive this session, so the merge does not run. That is the brief's
own sequencing, not a shortfall.

| # | prediction |
|---|---|
| **M1** | Block (N=100) wall **2100–4500 s**; point est. ~3200 s. **The anchor tests this directly.** |
| **M2** | Central (N=1000) wall **5.5–13 h**; point est. ~9 h |
| **M3** | **32 h ceiling not approached** — needs 384.4 ms/elementary at N=1000 against a measured max of 108.29. **Headroom 3.55×** |
| **M4** | RSS ~760 MiB @100, ~1.2 GiB @1000 (power law a=0.208); mechanism argues saturation *below* that |
| **M5** | Total **56–109 h** — see §1 |
| **M6** | Closure **2100 / 1500** |

**v2 is the reason the bands are wide, not carelessness:** v2's per-elementary
*fell* from 50→100 inputs (306.0 → 217.2, ratio 0.71). The step is a transition,
not an exponent, so a v3 block may cost *less* per elementary merge than the
50-input point. Three scenarios are carried rather than collapsed.

---

## 5. CLOSURE COUNTS — derived, and the table really does invert

`docs/CLOSURE_V3_PREREGISTRATION.md`.

| | **v3 (correct)** | v2 (the trap) |
|---|---|---|
| content | **2100** = 7 × 300 | 1800 = 6 × 300 |
| invariant | **1500** = 5 × 300 | 600 = 2 × 300 |

- **Content** = `closure: checked` **and** a histogram-like `root_class`. v3 adds
  exactly one: **`hFlavourClosureSpecies`**. 6 → 7.
- **Invariant** = `identity_checked: true`. v3 adds three: **`species_ordinal_schema`,
  `species_ordinal_labels`, `species_ordinal_digest`**. 2 → 5.

**Both counts grow, for different reasons** — which is why it inverts rather than
shifts. **A v3 run reporting 1800/600 has resolved against the v2 schema**: the
species sparse was never closure-checked and the provenance strings never
identity-checked. It would complete, report nothing, and have skipped exactly
what v3 exists for.

---

## 6. Owner errors, recorded (`docs/GATE_3000.md` §5)

- **"Size the gate's pool at P=4–8"** — a category error whose refutation was
  already in the record: **B10's own heading calls it "the serial checksum
  gate."**
- **"The 441 MiB figure is superseded"** — a cross-attribution. The gate's
  `ValidatePairDirectory` is **442.3 MiB ± 0.9 %, confirmed**; the merge's
  `MergeCanonicalAnalysis.C` went 571 → 657 MiB, superseded. Different programs.
- **Standing caveat ratified:** early samples here run slow. Pre-registrations
  quote their `n`; bands revise on record at **n ≥ 10**, beside the original.

---

## 7. NEXT SESSION

1. **Harvest the anchor** (`SCALING_100_ANCHOR_DONE`) against **M1**. It should
   land long before the gate.
2. **Harvest the gate** (`GATE_3000_DONE`, `gate_3000_report.json`) against
   `docs/GATE_3000.md` §3. **PASS ⇒ record actuals and proceed. Any failure ⇒
   item-STOP, failing directories enumerated, the merge does not run.**
3. **Take §1 to the owner** — the 56–109 h total against a 96 h ceiling. **This
   should be settled before the merge is launched, not during it.**
4. **The merge**, if GO survives §1. Detached, PIDs, sentinel, retention.
5. **Closure at scale** under §5's table, then the **first full-statistics
   species decomposition with block SEMs**.

---

## 8. COLD-READ SELF-REVIEW

1. **I burned the gate's first launch on an argument I never checked.** The
   campaign-relative `raw_path` is visible in one line of the manifest and I
   passed the parent directory anyway. It cost 0.75 s **by luck** — the raw-input
   check happens to run before anything expensive. The preflight I added is the
   right fix, but the honest description is that I got away with it.
2. **I wrote the assertion that killed the 10/25 re-run**, two sessions ago,
   pinning the checkout when I meant to pin the program. It was correct when
   written and expired without anyone noticing — including me, when I resubmitted
   the job *after* releasing the freeze that made it true.
3. **Three of my launches this arc have failed on their own guards or
   arguments** (the co-resident 10/25, the aborted 10/25, the gate's first run).
   None cost more than minutes, which is the system working — but the pattern is
   that I get the science right and the plumbing wrong.
4. **The merge's 56–109 h problem was found by reading `merge_root_files.sh`
   for a different reason** — I was after the block/central shape and noticed
   line 81. It would have been found at hour 96 otherwise. **Nothing in my
   process was aimed at it.**
5. **Every v3 scaling point is still n=1**, and M1–M5 rest on them. The anchor
   makes N=100 an n=1 point too. The standing caveat says bands revise at n≥10;
   **the merge will be launched on n=1 evidence** and that should be said plainly.
6. **The gate has no progress signal**, so "still running" is the most anyone can
   report for up to 33 h. If it dies silently mid-run, the sentinel is the only
   tell.

---

## 9. If you read only one thing

**The gate is running (`5399423`) after its first launch died on a wrong
`--production-root`; it emits nothing until it finishes, so check `condor_q`,
not the log. The merge is fully pre-registered and deliberately NOT launched —
and before it is, the owner needs to rule on §1: `merge_root_files.sh` runs the
gate itself, making the total 56–109 h against a 96 h site ceiling.**

---

## 10. APPENDED — the anchor inverted M3 before the session closed

**Appended, not rewritten.** At **n=21 of 300** the anchor's rate is **flat**
(mean 35.9 s/file, median 36.0) — it did **not** accelerate the way the 50-input
run did. n ≥ 10 revises the band on record, so:

| | registered | projected (n=21) | |
|---|---|---|---|
| **M1** block @100 | 2100–4500 s | **~10,757 s ≈ 3.0 h** | **MISS high 2.4×** |
| per-elementary @100 | — | **362.2 ms** | vs 108.29 at N=50 |
| **M3** central @1000 | ~9 h, headroom 3.55× | **30.15 h, headroom 1.06×** | **INVERTED** |
| **M5** total | 56–109 h | **201–213 h** | **vs 96 h ceiling** |

**The 50→100 step rises 3.34× in v3; in v2 it *fell* (0.71).** So the v3/v2 ratio
**crosses over between 50 and 100** — 0.354 at N=50, **1.668 at N=100**.

**Two criteria, and only one is breached.** The standing merge-strategy
escalation is `ratio ≥ 10`; 1.67 is nowhere near it, so **last session's "no
escalation" verdict stands on its own terms.** What breaks is the **wall-clock
budget**, which is a different question that no criterion was watching.

> ## ⛔ DO NOT LAUNCH THE MERGE ON THIS PLAN
> ~2.1–2.2× over the 96 h site ceiling, with the 32 h per-process ceiling
> simultaneously marginal at 1.06× — and that margin assumes per-elementary goes
> *flat* from 100 to 1000, which is precisely what the 50→100 step says it does
> not do. **Owner decision. Options all touch the production script.**

**Confirm against the anchor's final `SCALE` line first** — `5399458`,
sentinel `SCALING_100_ANCHOR_DONE`, ~3 h from its 20:42:30 start. If the second
half accelerates these numbers fall; 21 flat files is not what that looks like.

**§1's flag was directionally right and quantitatively far too kind:** it warned
~80 h against 96 h. The measurement says ~200 h.
