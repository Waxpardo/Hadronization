# HANDOFF v46 — the pipeline is built, staged and RUNNING; the numbers are ~3 h out

**Read `HANDOFF_20260811_v45.md` first; `docs/ERROR_RECORD.md` for the culture.**
This is the delta.

**No central numbers again — and again the blocker is the merge, not context.**
What changed: **the entire per-tune pipeline is now written, tested, and running
unattended on Nikhef for all three tunes.** Your first act is to harvest it, not
to build it.

**And the anchor defect turned out to be much broader than v45 recorded.** §2.

---

## 0. STATE, AS OF 2026-08-11 20:54 CEST

| | |
|---|---|
| **Local HEAD** | **`4ecd6b2`** on `physics-focus`, tree **clean**, suite **30/30** |
| **Nikhef HEAD** | **`43e35be`**, attached, **unmoved** |
| **THE MERGE** | **RUNNING**, PID `3675829`, **22 h 22 m**, **6 / 33** |
| **Guard** | pinfile **PRESENT**, hook **`2340a88f8fab9d5b`** |
| Condor | **0 jobs** — nothing pins the checkout except the merge |

### Three chains running unattended — **your harvest**

| tune | PID | waits for | then |
|---|---|---|---|
| **MONASH** | `1704916` | blocks 1–10 (~**23:45 Aug 11**) | closure → 11 extractions |
| **JUNCTIONS** | `1704639` | its blocks (~+10 h) | same |
| **CLOSEPACKING** | `1704640` | its blocks (later) | same |

Script `tools/pipeline/tune_chain.sh`, sha `eae4c0ae3b2dfaaa`. Output per tune in
`/data/alice/ipardoza/tune_runs/<TUNE>/`: `chain.log`, `manifest.txt`,
`central/per_species.csv`, `block_1..10/per_species.csv`.

**They fail closed.** If inputs never complete within 30 h the chain writes
`TUNE_CHAIN_ABORTED` and **does not run closure on partial input** — an early
version fell through and would have written a failing closure log that reads
like a physics result. Fixed before it could matter (§5).

---

## 1. WHAT TO DO WHEN A CHAIN FINISHES — the whole session in four commands

```bash
ssh stbc 'tail -30 /data/alice/ipardoza/tune_runs/MONASH/chain.log'   # closure verdict
scp -r stbc:/data/alice/ipardoza/tune_runs/MONASH  <scratch>/         # 11 CSVs + manifest
tools/decompose_with_block_sems.py <scratch>/MONASH --tune MONASH     # I3, I2, both conventions
```

**Read the closure verdict FIRST and stop there if it is not clean.**
Pre-registration: `docs/PER_TUNE_PROCESSING_PREREGISTRATION.md` (committed
before any tune finished) and `docs/CLOSURE_V3_PREREGISTRATION.md`.

> **The trap, restated because it is the one that would pass quietly: expect
> 2100 content / 1500 invariant. `1800 / 600` means the run resolved against the
> v2 sidecar schema instead of each file's own `analysis_schema` — that is
> FAILURE, not success.** I verified the frozen checkout's
> `ValidatePairBlockClosure.C:289` reads `analysis_schema` **from each file**,
> which is the correct path, so 2100/1500 is the expected outcome.

`tools/decompose_with_block_sems.py` runs the integrity gates **before** the
numbers: **I3** (ten blocks must sum to the central exactly, bin by bin — exact
arithmetic, not statistics) and **I2** (each block vs its central via the new
compare tool; false-positive expectation across all 30 comparisons is **0.16
bins**, so any flag is notable). **Flags ⇒ report before trusting the SEMs.**

---

## 2. ⚠ THE ANCHOR DEFECT IS BROADER THAN v45 SAID — corrected

v45 recorded "one bin, Σ̄_b⁻, ~10 σ". **That came from examining only the six
Σ_b bins.** `tools/compare_subset_parent.py` over **all 95 ordinals** flags
**30 of 88 testable bins at |z| > 4**, robust across three variance models
(binomial 30, Poisson 27, independent 26).

**And the deviations are physically large, not sub-percent differences inflated
by large N:** 16 exceed **2 %**, up to **33 %**, median **3.1 %**. **They are
almost entirely baryons** — Ω_b, Ω*_b, Ξ'_b, Ξ*_b, Σ_b, Σ*_b, Ξ*_c, Ξ'_c, Σ*_c —
while flagged *mesons* deviate sub-percent. **Ξ*_c⁺ (+6.3 %, z = +11.6) is
comparable to the bin the incident was named for.**

**Consequences, all applied:**

- the quarantine widens from *charge-resolved* to **the whole baryon sector**;
- **`docs/B_BARYON_ADVISORY_DIAGNOSTIC.md` is annotated superseded** — every
  ratio in it came from exactly that sector. **Its conclusion stands** because
  v45 re-measured it on the merged central and on 1000 files of raw records;
  **quote the raw-count table, not that one**;
- **aggregates remain sound** — the sector carries ~2 % of weight against charm
  mesons' ~90 %, which is why totals, the second-branch number and the map-v2
  table all agreed to < 0.02 pp. **Nothing published needs revision.**

> **The lesson, now E4 in the error record: aggregate agreement is not bin
> agreement.** Four independent aggregate checks passed on a dataset with 30
> bad bins.

---

## 3. TASK 1 DELIVERED — `tools/compare_subset_parent.py` + its test

Per-bin z-scores against an expected scale, |z| > 4 flagged. **Fail-closed on
non-integer contents** — the binomial variance is a counting model and would
manufacture significance on weights.

**Its test is the point** (`tests/test_compare_subset_parent.py`, in the suite,
now 30/30). Three checks that fail independently:

1. **known case** — reproduces the pinned **30-bin** reference set on the anchor
   vs merged central at scale 9.9986;
2. **negative control** — parent vs itself flags **nothing**;
3. **injected positive** — a synthetic 10 σ bin **is caught**.

Without (2) a tool that flags everything passes (1); without (3) a tool that
flags nothing passes (2). **The reference set is pinned, so a behaviour change
in the tool or either fixture fails loudly rather than quietly.**

---

## 4. ALSO DELIVERED

- **Σ_b raw-count leg FINAL at 1000 files:** Σ_b **26.59 % ± 0.24**, Σ*_b
  **10.51 % ± 0.19**, ground **0.83 % ± 0.11**. R1 HIT; **R3 for Σ*_b is now a
  HIT (+1.47 % ± 0.47, +3.2 σ)**; Σ_b null. R2 holds for every state carrying an
  asymmetry — **Λ_b⁰ is the exception and the expected one**, since at 0.83 %
  there is nothing to grow. All ten block logs anchored.
- **Merged MONASH central's per-species table committed** as
  `AnalysisScripts/anchors/merged_monash_central/` — closes v45 gap #3 and is
  the compare tool's parent fixture.
- **Anchor manifest + ERROR_RECORD E4** annotated per the brief.

---

## 5. WHAT I GOT WRONG THIS SESSION

- **v45's "one bin" was too narrow** (§2). I looked only where I already
  suspected. **Running the tool on its own known case is what corrected it** —
  which is the argument for building the tool before trusting the incident.
- **My chain's wait loop fell through on timeout**, and would have run closure
  on incomplete input. Fixed to `CHAIN_ABORT`.
- **I overwrote a script a running process was executing.** Restarted that chain
  rather than gamble on bash re-reading a changed file by byte offset. Its log
  confirmed no work was lost.

---

## 6. YOUR ORDERED QUEUE

1. **Harvest MONASH** (~23:45) — §1. Closure verdict first, then integrity, then
   the decomposition. **Then JUNCTIONS, then CLOSEPACKING**, same three commands.
2. **On full merge completion** (33 `PROMOTED_MERGE` **AND** PID exited): verify
   shape, **score the wall clock against 65–77 h**, remove the pinfile, **then
   advance the Nikhef checkout** — which deploys the guard/installer fix; verify
   hook sha and run its B-series tests live.
3. **Cross-tune table** — three tunes × both conventions with block SEMs. **The
   Σ_b physics gate is already passed (v45), so these are FINAL** provided
   closure is green and integrity clean per tune.
4. **Advisory step 2** — per-tune b-baryon ratios, one table, stop.
5. **Pin the review candidate** only when: suite green, tree clean, **no live
   processes**, all three tunes landed, provenance readable from the repo alone.

---

## 7. COLD-READ SELF-REVIEW

Traps handled: that the chains **fail closed** rather than producing a
plausible-looking closure failure (§0); that **1800/600 is failure** (§1); that
v45's "one bin" is **corrected here, not silently** (§2); and that the b-baryon
advisory's numbers are superseded while **its conclusion is not** (§2).

**What I could not resolve:** the central numbers, for the second session
running. The merge has produced **6 of 33 objects in 22.4 h** and MONASH's tenth
block is ~3 h out. **I chose to stage the pipeline and hand off rather than
start the resubmission's central number with ~15 % of context left** — the
brief's own instruction, and the alternative was a rushed job on the number the
paper turns on.

**Four sessions have now registered the 65–77 h band and none has scored it.**
It is the oldest open measurement in the project.
