# Handoff v19 — axis decision recorded, M6 closed, 6(iii) re-verified

**Date:** 2026-08-09. **Branch:** `physics-focus`.
**HEAD at time of writing:** `d6f451c` (local).
**Nikhef HEAD:** `e6429b7` — **FROZEN, unmoved.**

> ### CHECKOUT FREEZE STILL IN FORCE — HF_RUN3_V1 IN FLIGHT
> **2421 of 3000 jobs have not started** and verify their commit at startup.
> The checkout does not move until the campaign completes.

---

## 1. Campaign — healthy; the tune-index check is STILL unreachable

| | |
|---|---|
| Queue | **2421 idle, 144 running, 0 HELD** (read by reason) |
| Promoted | **MONASH 435**, JUNCTIONS 0, CLOSEPACKING 0 |
| Validator failures | none |

**~579 of 3000, still inside MONASH's block** (jobs 0–999).

**THE TUNE-INDEX SEED CHECK REMAINS OUTSTANDING** across four sessions now, for
the same structural reason: JUNCTIONS starts at job 1000 and CLOSEPACKING at
2000. **MONASH cannot exercise the tune term of the v2 formula.** On first
sighting: **JUNCTIONS job 0 ⇒ `131000001`**, **CLOSEPACKING job 0 ⇒
`132000001`**.

**Retry round not earned** — zero holds. The CR tunes carry essentially the
whole hang history (means 659 s / 989 s against MONASH's 377 s), so holds should
be expected once jobs 1000+ start. **`resubmit_held.py` remains untested on a
batch.**

---

## 2. THE AXIS DECISION IS RECORDED — M6 closes by construction

**Owner ruling: common absolute N_ch boundaries, one set for all three tunes and
both sectors, labelled as MONASH-MB percentiles, with the per-tune translations
published.** Recorded in `RELEASE_BLOCKERS.md` M6 and
`docs/PRODUCTION_SHAPE_DECISION.md` §5c (`673515b`).

**The boundaries** (half-integer, so no integer N_ch is ambiguous):

```
-0.5, 2.5, 3.5, 5.5, 6.5, 8.5, 10.5, 13.5, 17.5, 23.5, 32.5
```

Derived from MONASH MB, 172,429 accepted events, with realised percentiles and
their statistical errors recorded. **Realised values overshoot targets by up to
6.8 pp at c4** because N_ch is discrete — **the realised value is the one that
means anything.**

**The paper-facing translation table, max residual 2.91 pp:**

| class | bnd | MONASH | JUNCTIONS | CLOSEPACKING | spread |
|---|---|---|---|---|---|
| c2 | 2.5 | 11.80 % | 11.22 % | 11.86 % | 0.64 |
| c5 | 6.5 | 40.15 % | 37.24 % | 38.93 % | **2.91** |
| c11 | 32.5 | 91.58 % | 90.84 % | 92.10 % | 1.26 |

*(full 11-row table in the memo)*

**Recorded explicitly and worth re-reading before anyone quotes it:** this is a
**different test** from the escalation's and **must not be reported as "the
criterion now passes"**. §5b asked whether *per-tune* boundaries coincide — they
do not, 5/11 outside ±3 pp. This asks how far a *common* boundary's meaning
drifts between tunes. **First question settled negatively; second is the
published residual.**

**An off-by-one was caught before the table was recorded.** `FindBin` on a
half-integer edge returns the bin *above* it, so the first computation read
MONASH c2 as 19.40 % against the boundary derivation's 11.803 % — the same
quantity. **The corrected table reproduces 11.803 % exactly**, which is the
consistency check that caught it. **Anyone recomputing this table should
reproduce that number as their own check.**

---

## 3. 6(iii) RE-CHECKED under the new boundaries — Option A stands (`5d0b071`)

**Required by the ruling, not assumed.** Same extraction as `5ed3bc9`, new cuts,
**11 proper classes this time** — the earlier run merged c10/c11 through a
boundary-list error, fixed here.

**Binding values, mean entries/block at 1000 jobs:**

| tune | Λ_b | **Σ_b^±** | B-meson ctrl |
|---|---|---|---|
| MONASH | 258 | **32** | 3095 |
| JUNCTIONS | 241 | 344 | 2211 |
| CLOSEPACKING | 242 | 268 | 2139 |

**The binding value improved from 20 to 32** — common c1 `[−0.5, 2.5)` is wider
than MONASH's former `[0, 2)`, so the sparsest class gained population. **The
axis change strengthened the statistics case.**

- **Σ_b⁰ now passes everywhere** (MONASH min 22, was 7 failing c1). Still
  `centralEligible = false`, still unquoted — but a completeness table including
  it no longer inherits a starved class.
- **B_c unchanged in kind**: fails c1–c8 every tune. **Still
  multiplicity-integrated / top-class-only**, as G2 declared. Not rescued, never
  expected to be.

**The pre-registered failure branch did not trigger. No owner flag required.**

---

## 4. Stage-1 — F5 approved and folded; the species axis NOT started

**F5 (`d6f451c`):** `coexists_with` ratified, and the legibility condition is
now policy — **v3 outputs carry schema tags and provenance to the same raw
manifest, and every merge and plot consumer fails closed on a v2/v3 mix.**
Recorded *why* fail-closed rather than warn: coexisting views answer different
questions, so averaging them answers neither, and a warning is a thing an
operator scrolls past. Three concrete obligations written; **output-layout left
unwritten pending approval.**

**The species axis implementation is NOT started, deliberately.** Sized: four
sites in a 1384-line macro (`MakeClosure:623`, fill `:1008`, write `:1122`, plus
init), needing ordinal-table loading, a fail-closed PDG→ordinal map, and v3
schema tagging. **The brief requires both exact validations to hold — closure
sum rule at 1e-9 and species-axis totals reproducing the 6-category axis
bin-for-bin — with a mismatch being an item-STOP quantified, not patched.** With
the context left I would have landed code and not validation. **A half-validated
axis change is worse than none**, so nothing was begun.

**Ready for the next session:** ratified 202-ordinal artifact and generator
(`bec170f`), fixture verified byte-identical
(`49657c2c…3651`), and the sizing above.

**Item 4 (F3/F4) not started.**

---

## 5. Cold-read self-review

1. **The translation table's ≤2.91 pp is not a passing grade on the escalation.**
   I have said so in three places because it is the single most likely thing to
   be misread — the two tests answer different questions and only one of them
   was ever a gate.
2. **The off-by-one I caught was in my own first computation**, and only the
   cross-check against an independently derived number exposed it. **Had I
   computed the translation table alone, without the boundary derivation beside
   it, it would have gone into the record wrong** — the two tables sharing a
   quantity is what saved it.
3. **The 6(iii) re-check used 11 proper classes where the original merged
   c10/c11.** So the improvement from 20 → 32 is partly a fairer comparison and
   partly the wider c1; **I did not separate the two contributions**, and the
   verdict does not depend on doing so.
4. **The tune-index seed check has now been outstanding for four sessions.** It
   is not neglect — the jobs genuinely have not run — but it is the last
   unverified part of the seed formula and the campaign is 19 % done.
5. **I started nothing I could not finish.** Item 3's axis and item 4 are
   untouched rather than partial.
6. **No promoted data, tune card, `config/`, generated header or seed ledger was
   touched. Nikhef checkout unmoved at `e6429b7`.**
