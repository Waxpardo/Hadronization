# HANDOFF v45 — Σ_b adjudicated (PHYSICS), Task 1 retracted, closure still blocked

**Read `HANDOFF_20260811_v44.md` first; v43 for the maps; `docs/ERROR_RECORD.md`
for the culture.** This is the delta.

**The session did NOT deliver the central numbers, and the reason is not
context: the merge is at 5/33 and closure needs ten blocks per tune that do not
exist yet.** What it did deliver is the Σ_b adjudication, which was gating the
finality of those numbers — so when they are computed they can be recorded as
**final**.

---

## 0. STATE, AS OF 2026-08-11 19:59 CEST

| | |
|---|---|
| **Local HEAD** | **`fec5dc1`** on `physics-focus`, tree **clean**, suite **29/29** |
| **Nikhef HEAD** | **`43e35be`**, attached, **unmoved** |
| **THE MERGE** | **RUNNING**, PID `3675829`, **21 h 27 m**, **5 / 33** |
| **Guard** | pinfile **PRESENT**, hook **`2340a88f8fab9d5b`** |
| **Σ_b raw jobs** | cluster `5449994`, **8/10 harvested**, 2 still running — **these pin `43e35be`** |

**The freeze stays armed.** Pinfile removal needs 33 `PROMOTED_MERGE` **AND** PID
exited. **Neither Task 3 (harvest/advance) nor Tasks 4–6 could start.**

---

## 1. ⚖ TASK 2 VERDICT: **PHYSICS CONFIRMED** — Task 5 may be recorded FINAL

`docs/SIGMA_B_ORDERING_AND_ADJUDICATION.md` carries the pre-registration
(committed before measuring) and the results.

**Task 2.2 — the readers agree EXACTLY.** Independent sparse-cell walk vs the
extraction reader on the merged MONASH central: 95 ordinals both, totals equal
at 1,298,655,240, **zero differing bins**, every b-baryon bin equal. Projection
machinery exonerated.

**Task 2.1 — the raw record, 800 MONASH files, no analysis chain at all:**

| group | *A* |
|---|---|
| spin-½ Σ_b | **26.71 % ± 0.26** |
| spin-3/2 Σ*_b | **10.62 % ± 0.21** |
| ground (Λ_b, Ξ_b) | **0.83 % ± 0.12** |

| # | prediction | result |
|---|---|---|
| **R1** spin-sorted | **HIT, decisive** — tiers separated by ~100 σ |
| **R2** grows toward forward \|η\| | **HIT, decisive** — Σ_b⁺ 18.0 → 21.9 → 31.1; every species monotonic |
| **R3** uub > ddb | **Σ*_b HIT (+1.60 % ± 0.52, +3.1 σ)**; Σ_b null (+0.24 % ± 0.65) |

> **R2 is the finding that settles it.** The asymmetry roughly **doubles** from
> central to forward |η| for every Σ_b state, in the generator record itself.
> That is the beam-remnant signature, and no counting error produces it.
> Raw magnitudes match what the extraction reports, so **the machinery
> transmits faithfully rather than manufacturing.**

---

## 2. ⚠ TASK 1's MISS IS RETRACTED — and the anchor is why

Task 1 (on the anchor) said the ordering was **exactly reversed at −7.4 σ**.
**Better data contradicts it.**

| dataset | A(uub) − A(ddb) | |
|---|---|---|
| **anchor** (no recorded provenance) | **−6.17 % ± 0.84 %** | −7.4 σ |
| merged MONASH central (1000 inputs) | **+1.35 % ± 0.27 %** | **+5.1 σ** |
| raw counts (800 files, no chain) | +0.24 % ± 0.65 % | null, same sign |

**The anchor disagrees with the merged central at 8.6 σ, and it localises to one
bin.** Scaling anchor→merged is 9.76–10.33 for five of six Σ_b bins and **9.06
for Σ̄_b⁻ alone**; at a scale factor of **9.999** that bin sits **~10 σ high**.

> **The only dataset in this project with no recorded provenance is the one that
> disagrees, and it disagrees in a single bin.** Two traceable datasets give the
> predicted sign.

**The anchor's AGGREGATES are sound** — recomputed on the merged central,
second-branch moves 12.8451 → **12.8396 %** and map-v2 D⁰ 25.2425 → **25.2435 %**.
**Nothing published needs revision.** But **do not use the anchor for anything
charge-resolved again**; Task 5's fresh extraction supersedes it.

---

## 3. WHY TASKS 3–6 DID NOT RUN — a hard blocker, not a budget choice

**`Validation/validate_pair_block_closure.sh` requires
`combined_root_1` … `combined_root_10`** — all ten blocks of a tune. The merge
has promoted the MONASH central plus **blocks 1–4**. **Closure cannot run**, and
Task 5's block SEMs need the same ten blocks per tune × three tunes.

**Merge order observed:** central first, then blocks. **MONASH needs ~6 more
block cycles before closure is possible for even one tune.**

**Task 6** (per-tune advisory) needs three-tune merged output — further still.

**Closure's pre-registration already exists** (`docs/CLOSURE_V3_PREREGISTRATION.md`,
C1–C4) — no new pre-registration needed. **Remember: 2100 content / 1500
invariant. 1800/600 means FAILURE on v3**, and C4 says the first thing to check
is whether the run resolved the object set from each file's own
`analysis_schema` rather than the sidecar JSON.

---

## 4. TASK 7 — review readiness advanced substantially

`AnalysisScripts/anchors/` now holds, each with a manifest:

| anchor | anchors | verified |
|---|---|---|
| `extraction_dual/` | the pre-merge tables | **provenance unrecoverable**, stated plainly |
| `f4_probe/` | maps v1, v1.1, v2 | **rebuild reproduces `dd502a10c5932fff…` exactly**; C7 byte-identical |
| `m7b_blocks/` | the beauty-M7 table | reproduces 0.0983 / 0.1023 / 0.0115 |
| `sigmab_raw/` | the Task 2.1 raw counts | 8 logs, each with macro sha and file count |

**Every one was verified to reproduce its published result from the committed
copy**, not asserted.

### Still failing the "readable from the repo alone" test

1. **The merge log and run manifest** — merge still running; **commit at
   completion**. `docs/MERGE_V3_BAND_VALIDATION.md` remains the timing record
   because **the log has no timestamps**; those numbers are reconstructed from
   filesystem mtimes and die with scratch.
2. **`extraction_dual` provenance** — unrecoverable; superseded by Task 5.
3. **The merged-central reader output** used in §1–§2 lives in
   `sigmab_runs/task22/` scratch. Small; **anchor it or regenerate under Task 5.**

**Review candidate: `fec5dc1`** — suite 29/29, tree clean. **But it is not yet a
good candidate**, because the central numbers (closure, decomposition) do not
exist and two live processes remain. **The candidate should be pinned after the
merge lands and Tasks 4–5 complete.**

---

## 5. YOUR ORDERED QUEUE

1. **Harvest the last 2 Σ_b blocks** (cluster `5449994`) — optional, changes
   nothing structural — then **they stop pinning `43e35be`**.
2. **Watch the merge by counts.** Band already validated (v44); **do not
   re-validate, do not project.**
3. **When MONASH has 10 blocks: run closure for MONASH** — it is per-tune and
   does not need all three. Then the other tunes as they land.
4. **On merge completion:** verify shape (3 centrals @1000, 30 blocks @100),
   score wall clock against 65–77 h, remove the pinfile, **then advance the
   Nikhef checkout** — which deploys the guard/installer fix; verify hook sha
   and re-run its B-series tests live.
5. **Task 5** — fresh extraction with a full provenance manifest, three tunes ×
   both conventions, block SEMs (nonlinear quantities formed inside blocks,
   dof=9). **Record as FINAL** — §1 clears it. **Compare against the anchor for
   MONASH and record the comparison** (§2 gives the expected agreement).
6. **Task 6** — per-tune b-baryon ratios from the three-tune extraction.
7. **Pin the review candidate** once §4's three gaps close.

---

## 6. COLD-READ SELF-REVIEW

Traps handled: that Task 1's headline MISS is **retracted later in the same
document** (§2 — read to the end before quoting it); that closure is blocked by
a **ten-block requirement**, not by time (§3); that the anchor's aggregates are
fine even though one of its bins is not (§2); and that `fec5dc1` is *a* clean
commit but **not yet the review candidate** (§4).

**What I could not resolve:** the central numbers. Closure and the species
decomposition are exactly where v44 left them, because the merge has produced
5 of 33 objects in 21.5 h and the tenth MONASH block is several hours out.
**Three sessions have now registered the 65–77 h band and none has scored it.**

**And one thing I would flag to the owner:** the anchor's bad bin was found only
because Task 1 was run on it and Task 2 was run on something else. **No check in
the pipeline compares a subset against its parent.** If the anchor had never
been questioned, that bin would have gone to external review inside a committed
artifact.
