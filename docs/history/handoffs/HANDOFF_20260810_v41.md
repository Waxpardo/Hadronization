# HANDOFF v41 — the gate PASSED and THE MERGE IS RUNNING

**Read `HANDOFF_20260810_v40_GENERATIONAL.md` first; it is still the entry
point.** This is the delta. The one thing that changed everything: **the gate
returned PASS and the merge launched at 22:32 CEST — the first time in eight
sessions.**

---

## 0. STATE, AS OF 2026-08-10 22:35 CEST

| | |
|---|---|
| **Local HEAD** | **`bcdc587`** on `physics-focus`, tree clean |
| **Nikhef HEAD** | **`43e35be`** on `physics-focus`, attached, **unmoved** |
| **THE MERGE** | **RUNNING. PID `3675829` on `stbc-i3.nikhef.nl`**, started 22:32:14 |
| **The gate** | **DONE, PASS.** Cluster `5399423` terminated 15:26:31, rc=0 |
| **M7 beauty** | **RUNNING.** Condor cluster **`5425788`**, 10/10 blocks, all RUNNING |
| Guard | **REFUSES** `can-advance` — correct, but see §5, it is refusing for the wrong reason |
| Suite | **not re-run this session** — see §6.4, an honest gap |

### Live things, exactly

```
merge   detached  PID 3675829   stbc-i3   log merge_runs/HF_RUN3_V1_merge/merge_v3.log
M7b     condor    5425788.0-9   10 RUNNING, 0 done
orphan  PID 3669337  a hung `condor_history 5399423` of mine, ~4 h, harmless
```

**A note on this session's clock.** Orientation ran at 03:29 and the merge
launched at 22:32; ~19 h of wall clock elapsed inside one context window. If you
inherit timings from me, check them against `date` rather than assuming they are
minutes old.

---

## 1. THE GATE'S VERDICT, VERBATIM

From `/data/alice/ipardoza/gate_runs/gate_3000.log`:

```
ANALYSIS_OUTPUT_MANIFEST_VALID status=PASS directories=3000 missing=0 commit=61fe978f66c00e8467f88c00d677462292dd5a1c
# rc=0
# time=[67561.53 54133.71 5586.89 453596 ] (wall user sys maxrssKB)
# finished=2026-08-10T15:26:31+02:00
```

Sentinel `gate_runs/GATE_3000_DONE` contains `rc=0`.

**Re-read independently after the ssh that first fetched it died (exit 255, on a
hung `condor_history`), because a launch this consequential should not rest on a
transport that failed.** Identical both times; the log is complete at 11 lines.
**`sha256(gate_3000.log) = 72e4c308fd745b60be1c5014076d3c84f9438edf49ff1de9115722f090ed5e4e`**
— confirm it is unchanged before trusting my transcription of the verdict.

**Against the pre-registration: `status=PASS directories=3000 missing=0` — an
exact match on all three fields.** Wall **18.77 h** against ~19.3 h registered,
**3 % low**. G1/G2 read as MISSES LOW exactly as v40 said they would.

**Method note:** the job left the queue between two of my checks and
`condor_history` returned nothing for it, which reads alarming. **The job event
log is the authority** — `condor_gate.log` records `005 ... Job terminated. (1)
Normal termination (return value 0)`, `TimeExecute 67566 s`. Do not conclude a
job died because `condor_q` is empty and `condor_history` is silent.

### The frontier probe, scored

At 03:30 I pre-registered frontier 960–970 from v40's slot_660 @ 01:36:15 and
22.41 s/dir. **Measured: MONASH slot_972. HIT.** Pooled 22.28 s/dir, local rate
21.8 s/dir, ETA ~16:04 against an actual 15:26.

> **One correction to the probe method, and it matters.** The **count** of
> stamped directories (934) is **not** the frontier (972). Under `relatime` a
> directory already read within 24 h with atime ≥ mtime is **not re-stamped** —
> `slot_970` still carried a 19:00 atime from an earlier read while 969, 971 and
> 972 were fresh. **Use the maximum index, never the count.** The count
> undercounts by however many directories a previous pass happened to touch.

---

## 2. THE MERGE — WHAT IS RUNNING AND HOW TO RESUME IT

**Invocation, recorded in `merge_runs/HF_RUN3_V1_merge/manifest.txt`:**

```
merge_root_files.sh \
  /data/alice/ipardoza/Hadronization/campaigns/HF_RUN3_V1/freeze \
  /data/alice/ipardoza/hadronization_production/HF_RUN3_V1 \
  /data/alice/ipardoza/hadronization_analysis/HF_RUN3_V1 \
  /data/alice/ipardoza/hadronization_merged \
  HF_RUN3_V1
```

Launcher: `merge_runs/merge_v3_launch.sh` (**re-run it verbatim to resume**).
`merge_script_sha=75708821b3cf9204…`, `canonical_manifest_sha=fcd96eaebd4dc11f…`,
df at launch **1.1 T** against 84–108 GB needed.

**Positive check passed before I called it launched:**

```
CANONICAL_MERGE_SHAPE inputs_per_tune=1000 inputs_per_block=100 events_per_tune=100000000
```

That is the registered shape — 3 centrals @1000, 30 blocks @100, 33 `merge_one()`
calls — and the process args confirm `production_root` is the **campaign**
directory, which is the specific mistake that cost a run before.

### What it is doing right now, and why nothing will appear for ~19 h

**`merge_root_files.sh:80-83` runs the full gate again, unconditionally**, before
any merging. That is ~18.8 h of the 65–77 h, measured. **A quiet log for most of
a day is expected, not a hang.** First `PROMOTED_MERGE` should appear roughly
19 h after 22:32, i.e. **around 17:30 on 2026-08-11**.

### ⛔ Do not run `validate_analysis_outputs.py --report` by hand

The merge's own internal call is the designed path: it writes
`analysis_output_manifest_validation.json`, whose sha256 every promoted
directory records in `merge_provenance.json`. **Rewriting it fails all 33
directories** (`docs/PHASE4_SUBMISSION_CHECKLIST.md:70-92`). I confirmed the
report was **absent** before launch, so there is no overwrite conflict.

The prohibition and the merge do not conflict — **the ban is on invoking it
yourself**, not on the merge's internal call. I nearly stopped over this; the
resolution is written down so you do not have to re-derive it.

### Bands to score the FIRST completed block against — not before

- first block `PROMOTED_MERGE` wall **2643 s ± 30 %**;
- merge-side child RSS **~836 MB**;
- centrals ~7.3 h each (extrapolation, labelled as one);
- total **65–77 h** against the 96 h ceiling.

**Then counts only.** `merge_one()` unit costs are **ordered**, so every prefix
lies pessimistically — v40 §5 records an escalation built on exactly that error.
The per-file mean falls 35.9 → 8.8 s across a single block. **Never project.**

---

## 3. WHAT THE THREE TASKS DELIVERED

### 3.1 Task 3 — the second-branch number: **12.84 %** (commit `d20b46f`)

`docs/SECOND_BRANCH_WEIGHT.md`, `tools/second_branch_weight.py`.

**The decision rule fires its "more" branch, ~13× over the ~1 % threshold. The
convention is NOT changed — that is the owner's call.** Three options with costs
are in §5 of that document; **my recommendation is option 3** (probe all
channels, quantify the true migration, then decide) because it replaces a bound
with a measurement and cannot destabilise the reader.

- **The brief and v40 specified different quantities.** Measured both rather
  than picking: **12.8400 %** expected misassigned weight (brief), **35.7910 %**
  weight merely exposed to a substantial second branch (v40). Both ≫ 1 %, so the
  decision is the same either way.
- The reader's walk is **recursive**, so I also computed the BR-product form:
  **12.8451 %**, i.e. **+0.005 pp**. Negligible, because 48.19 of the 48.20 %
  that is reassigned travels exactly one hop. Checked, not consequential.
- **97.81 % of the effect is the four D\* charge states.** A BR-split is four
  species, not 202.
- **It is an UPPER BOUND and is labelled as one.** (1−BR_dom) is the chance of
  another *channel*, not another *bin*, and the map records **only** dominant
  products — there is no subdominant daughter in the artifact to follow. The
  next step is a **map v2** from `tools/f4_probe.cc` retaining all channels.

**Positive check:** the reimplementation reproduces the published table exactly
(D⁰ 59,678,352 / 45.9475 %, D⁺ 13,331,304, B⁰ 5,042,102) on weights summing to
**129,883,844**. The tool fail-closes and prints nothing until that passes.
**Scope: MONASH anchor, four directories — not three-tune.**

### 3.2 Task 2 — beauty M7, in flight (commit `bcdc587`)

`Validation/MeasureUnresolvedSystematic.C` now takes a **sector** argument,
default `"c"`, so every existing charm invocation is byte-identical. Unknown
sector **fails closed** rather than falling through to charm.

**Deployed to scratch `/data/alice/ipardoza/m7b_runs/`, not into the frozen
checkout.** Driver `m7b_block.sh`, submit `m7b_block.sub`, cluster **`5425788`**,
ten blocks, `Requirements = (Machine != "wn-sate-072...")` retained.

**Pre-registration is committed BEFORE the run** — `docs/M7_BEAUTY_PREREGISTRATION.md`,
structure-only, no numeric prediction.

**Pre-flight, all passed on one file:**

| check | result |
|---|---|
| **P1 charm regression** | **EXACT** — parametrised `sector=c` reproduces the frozen macro field for field |
| **P3 beauty distinct** | resolved_n 27,806 vs charm 197,743 — the sector took effect |
| xcheck both sectors | counts macro agrees with percentage macro |
| **P2 aggregator regression** | modified `aggregate_m7.py` reproduces the published charm table exactly: **0.0847 / 1.1530 / 1.1355 %**, shifts **0.0451 / 0.5497 / 0.5125 %** |

**One real bug fixed in the aggregator:** it `float()`s every token it does not
exclude, so a `sector=b` token would have aborted the whole run on `float("b")`.
Excluded; absent now means charm; it additionally fail-closes on pooling sectors.

**n=1 structural read (one file per tune), which is why launching was justified:**

| tune | beauty unres | beauty res | rate % | beauty baryon % |
|---|---|---|---|---|
| MONASH | **0** | 27,806 | **0.0000** | 4.89 |
| JUNCTIONS | 35 | 27,380 | 0.1277 | **32.01** |
| CLOSEPACKING | 29 | 27,606 | 0.1049 | **32.78** |

**B1 holds in its strongest possible form: MONASH is exactly zero.** I checked
the CR tunes before submitting precisely because an all-zero result would have
made the ten-job run pointless.

> **Watch for this when you aggregate:** if MONASH's unresolved count is zero in
> every block, its `relative_shift_pct` is **0 with a block SEM of exactly 0**.
> That is a real result — "MONASH beauty carries no unresolved-origin
> systematic" — **not** a broken run. The aggregator's guards handle unres=0.
> The beauty baryon fraction being **~6.5× higher in the CR tunes** (4.9 % →
> 32 %) is the paper's beauty-baryon story showing up directly.

**To harvest:**

```bash
python3 tools/aggregate_m7.py /data/alice/ipardoza/m7b_runs/block_*/m7b_block_*.log
```

It **fail-closes below ten blocks**. Expect a `SECTOR beauty (sector=b)` header.

### 3.3 Task 1 — done: gate harvested, merge launched. §1 and §2.

---

## 4. WHERE THE LOCAL CHECKOUT ACTUALLY IS — read this before you `cd`

**The brief said "local checkout at `6b97ccd`, clean". My shell's working
directory was `main` at `11884cf`, dirty, with no `docs/`, `tools/`, `config/`
or `Validation/` at all.** That is not a contradiction and nothing is wrong:

```
/Users/wax/Documents/Research/Projects/Hadronization                 main @ 11884cf   (paper/plotting view, dirty)
/Users/wax/Documents/Research/Projects/Hadronization-physics-focus   physics-focus    <- THE PROJECT LIVES HERE
```

**All of this session's work is in the `-physics-focus` worktree.** `main` is an
ancestor of `physics-focus`, so v40 was readable with `git show` without moving
anything — which is how I read it before touching a single file. **Do not "fix"
the main worktree's dirty state; it is someone else's view.**

---

## 5. ⚠ A REAL SAFETY GAP I FOUND AND DID NOT FIX

**`make can-advance` cannot see the merge.**
`tools/checkout_advance_guard.py:100` builds its evidence from `condor_q`. **The
merge is a detached `nohup` process on `stbc-i3`, not a Condor job.** Right now
the guard refuses — but it refuses because **M7b's ten jobs** are in flight.

> **When M7b finishes (~20 min from now), the guard will report ALLOWED while
> the merge still reads the frozen checkout for another ~60 h.** Advancing then
> would break the run — silently, hours later, exactly the failure mode
> `docs/PHASE4_SUBMISSION_CHECKLIST.md` warns about for the report.

**Treat the checkout as frozen until the merge's 33rd `PROMOTED_MERGE`,
regardless of what the guard says.** I did not patch the guard because it is a
safety mechanism and changing one while a 65 h run depends on it is not a call I
should make unsupervised. **This is an owner decision and it is time-boxed: it
matters the moment M7b converges.**

---

## 6. WHAT I GOT WRONG, AND WHAT I LEFT UNDONE

### 6.1 I nearly STOPped the merge over a prohibition that did not apply
The `--report` ban and `merge_root_files.sh:80-83` look like a head-on conflict.
Reading the rationale rather than obeying the surface rule resolved it in one
grep. **The rule protects promoted directories from a hand re-run; the merge's
internal call creates the very artifact being protected.** Written into §2 so
nobody re-derives it under time pressure.

### 6.2 I nearly launched against a standing "MUST NOT LAUNCH"
`docs/MERGE_V3_PREREGISTRATION.md` §5 still carries **"⛔ THE MERGE MUST NOT BE
LAUNCHED ON THIS PLAN"** in bold. It is **withdrawn in full** eighty lines
later. It is left standing on purpose because the error is the lesson — but a
cold reader who greps for a blocker will find the retracted one first. **Check
for a withdrawal before acting on any escalation in that file.**

### 6.3 Sizing: the 34 s that was not a regression
The first ROOT call on a raw file took **34 s** against the 3.32 s charm anchor —
a 10× miss that would have been worth escalating. It was **cold NFS cache plus
ROOT startup**: the next four calls averaged **3.75 s**, and the CR check ran two
calls in 6 s. **n=1 on a cold file is not a rate.** Not escalated.

### 6.4 What I did not do
- **The validation suite was not re-run.** v40 recorded 29/29; I inherited that
  number and never verified it. It is the one orientation claim I did not check.
- **Closure at scale not started** — correctly, it needs the merged output.
  **Remember the v3 table INVERTS: 2100 content / 1500 invariant. 1800/600 is
  FAILURE, not success.**
- **The merge's first block is unvalidated** — it cannot exist for ~19 h.
- **M7b produced no numbers yet**; blocks were still running at handoff.

---

## 7. YOUR ORDERED QUEUE

1. **Harvest M7 beauty** (~20 min out at handoff). `aggregate_m7.py` over the ten
   block logs; expect the `SECTOR beauty` header and n=10 or a fail-close. Score
   against `docs/M7_BEAUTY_PREREGISTRATION.md` §2 — **structure only**, and if
   MONASH comes out above the CR tunes **report it as a finding**. Then write the
   beauty table beside the charm one.
2. **Watch the merge by counts, at long intervals.** Nothing before ~17:30 on
   2026-08-11. Validate the **first completed block** against 2643 s ± 30 % and
   ~836 MB RSS, record hit/miss, **then stop projecting**.
3. **Re-run the validation suite** and confirm 29/29 — close §6.4.
4. **Put §5's guard gap and Task 3's BR-split decision to the owner.** Both are
   owner calls; the guard one has a ~20-minute fuse.
5. **After the merge lands:** closure at scale under the **inverted** v3 table,
   then blocks → SEMs on the species decomposition, both conventions, three
   tunes. That is the resubmission's central number.

**If the merge dies:** re-run `merge_runs/merge_v3_launch.sh` verbatim. It is
resumable by validation, not by markers, and it **refuses to overwrite**. **Never
delete a partial output.** Every resume re-pays the ~19 h internal gate.

---

## 8. COLD-READ SELF-REVIEW

Read back as someone who knows nothing. What I would have tripped on, now fixed
above: that my shell's cwd is **not** the project worktree (§4); that a silent
merge log for 19 h is the internally-run gate and not a hang (§2); that the
`--report` ban does not block the merge (§2, §6.1); that a bold MUST-NOT-LAUNCH
in the merge doc is retracted further down (§6.2); that `condor_q` being empty
is not evidence a job died (§1); and that the atime **count** is not the
frontier (§1).

**What I could not fix:** I never saw a single `PROMOTED_MERGE` line. Everything
in §2 downstream of `CANONICAL_MERGE_SHAPE` is a prediction, including the ~19 h
quiet period. **If the log is still empty 24 h after 22:32, that is past my
prediction and worth investigating rather than continuing to wait.**
