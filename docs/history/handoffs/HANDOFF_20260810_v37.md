# Handoff v37 — F3 COMPLETE and green; gate ~16 h from PASS; merge still queued behind it

**Read v21 (`48cf3a3`) for generational context; v36 for the extraction reader.**

| | |
|---|---|
| **Local HEAD** | **`345c63c`** on `physics-focus` |
| **Nikhef** | **`43e35be`** — frozen by the in-flight gate; guard and hook refuse |
| **The gate** | **RUNNING — `5399423`.** Frontier **MONASH slot_400** at 23:59:54; **22.53 s/dir**; ETA **2026-08-10 ~16:15** |
| **The merge** | **NOT LAUNCHED** — gate PASS is ~16 h out. Pre-flight standing |
| **F3** | ✅ **COMPLETE. Sum rule exactly 1 on all twelve; reading is AGREEMENT** |
| Extraction reader | Validated on **four** directories now, all `AGREE` at 0.000e+00 |
| Suite | **29 / 29** |

---

## 1. F3 IS DONE, and it is green

`docs/F3_VIRTUAL_TRIGGER_CLOSURE.md` §7. Run `5401169` under the ruled
predicate; diff `f3_runs/f3_virtual.diff`, **79 added / 6 removed**, three edits.

### The sum rule met the pre-registration exactly

**Pre-registered: full-phase-space closure = 1 exactly for every virtual species;
any deviation is an item-STOP.** **All twelve give exactly 1**, and so does every
ground-state trigger in the same run (0 exceptions). **No item-STOP — the
record's copy structure contains nothing unmodelled.**

### The table — trigger-side completeness

| | mean | range | spread |
|---|---|---|---|
| ground-state beauty mesons | **0.8686** | 0.8575–0.8809 | 0.0234 |
| virtual B* (non-strange) | **0.8665** | 0.8610–0.8727 | 0.0117 |
| **difference** | **−0.0022 (−0.25 %)** | | |
| ground-state Λ_b | **0.8686** | 0.8680–0.8692 | 0.0012 |
| virtual Σ_b* | **0.8796** | 0.8056–0.9412 | 0.1356 |
| **difference** | **+0.0110** | | |

Per-species counts and values are in §7c of the F3 document.

**The reading is AGREEMENT**, the pre-registered branch:

- **B\* vs B: −0.25 %** — an order of magnitude below the ground states' own
  charge-state spread (0.0022 vs 0.0234). No trigger-side effect at B*
  statistics (~3 k triggers per charge state).
- **Σ_b\* vs Λ_b: +0.011, unresolvable.** With n = 23–39 the approximate counting
  σ is 0.052–0.068 against an observed half-range of 0.068. **Exactly what
  statistics alone predicts.**

**⇒ The one-sentence trigger-side answer is supported:** the compensation
decomposition does not depend measurably on whether the trigger is a ground state
or an excited state of the same flavour, at the statistics available.

**My pre-registration hit on both halves** — agreement for B*, Σ_b* too thin.

### What it does NOT establish, stated in the document and repeated here

- **No block-level spread.** One file is one number per species; the quoted σ is
  a **counting approximation on a weighted sum**, not a block SEM.
- **The Σ_b\* result is a non-observation at n ≈ 30**, not a measurement of
  agreement. Full statistics reaches ~257 k, where it would constrain something.
- **Junction topologies are not separated out** — and that is the specific
  mechanism that motivated F3. This table is inclusive.

---

## 2. THE OWNER'S PHYSICS CORRECTION — verified, and my rationale was wrong

**Ruling:** decays are off for every heavy hadron in this record, so no B* has a
daughter; the real hazard is non-final event-record **copies**, which keeping
`isFinal` eliminates. **The descendant walk is removed.**

**Verified against the input before rebuilding** — of 11,440 events containing
B*/Σ_b*:

| check | events |
|---|---|
| `isFinal == 0` | **0** |
| a daughter link | **0** |
| sector charge `qb == 0` | **0** |

And the producer says it outright: heavy hadrons *"are final only because their
decays were disabled"* (`heavyflavourcorrelations_status.cpp:1026-1029`).

**My v36 rationale — "B* → B γ at ~100 %, the daughter carries the same b quark"
— is right about PYTHIA and wrong about this record.** The walk guarded a hazard
that cannot occur here. Removing it took the diff from five edits to three.

**The remaining blocker was the one already identified:** `heavyCentral` *is*
`FindGroundState` precomputed, so dropping one while keeping the other relaxed
nothing. Confirmed empirically — **all 12,181 events with a virtual species have
`heavyCentral == 0`**, while all are direct-primary and 12,029 are
`kSelectedHard`. Every other leg of the predicate already passed.

---

## 3. THE MERGE — unchanged, and still the long pole

Gate frontier **slot_400 of 3000** at 23:59:54, **22.53 s/dir** overall,
**ETA 2026-08-10 ~16:15**. Pre-flight stands: resume protocol recorded (*re-run
the same command*; every resume re-pays the 21–33 h internal gate), `df` PASS
(1078 GB vs 108 GB worst case), clean start, target
`hadronization_merged/` tag `HF_RUN3_V1`.

**On PASS:** production invocation, detached, PIDs and sentinel, retention, one
bounded first-block check against **2643 s ±30 %**, then counts only.

---

## 4. EXTRACTION READER — the one-directory gap is closed

v36's self-review flagged that the reader was validated on exactly one directory.
It is now run on **four** — the 10-, 25-, 50- and 100-input merged stages:

| directory | pairs | filled ordinals | self-check |
|---|---|---|---|
| `stage_MONASH_10` | 300 | 88 | **AGREE 0.000e+00** |
| `stage_MONASH_25` | 300 | 88 | **AGREE 0.000e+00** |
| `stage_MONASH_50_batch` | 300 | 88 | **AGREE 0.000e+00** |
| `stage_MONASH_100_anchor` | 300 | **91** | **AGREE 0.000e+00** |

**Exact agreement at every input scale**, and the filled-ordinal count rises
88 → 91 with statistics, as it should. **Still MONASH-only and still
single-tune** — the three-tune, 1000-input product is what the merge will
provide.

---

## 5. NEXT SESSION

1. **Harvest the gate** — ETA ~16:15 today. **Pre-registered `status=PASS
   directories=3000 missing=0`**, wall ~19.3 h. G1/G2 read as misses low, already
   recorded. **Any failure ⇒ item-STOP, enumerated, nothing launches.**
2. **Launch the merge on PASS.** Re-check `df`, bounded first-block check, then
   counts only — **no projections** (the workload's unit costs are ordered).
3. **F3's table to the owner** — it is complete and needs a decision on how it
   enters the manuscript, not more work.
4. **Merge completion spans sessions** (~65–77 h). Then **closure at scale** under
   the v3 table (**2100 content / 1500 invariant**), then the ten blocks land and
   the extraction reader gets its **SEMs** — the species decomposition with
   uncertainties, the resubmission's central number.

---

## 6. COLD-READ SELF-REVIEW

1. **I verified the owner's correction against data before acting on it**, and it
   was right — but the check that settled it (do B* have daughters?) is the same
   check that would have prevented my wrong rationale in the first place. **One
   query on the input, before writing the walk, would have saved edits D and A
   and two of last session's five iterations.** The regret from v36 is unchanged,
   only better evidenced.
2. **F3's conclusion rests on a single 100 k-event file.** The B* half is solid;
   the Σ_b* half is a non-observation I have been careful to label as such, but a
   reader skimming the table will see two rows that look equally supported. The
   document says so; the handoff table above says so; it will still be misread if
   anyone quotes the means without the counts.
3. **I did not separate junction topologies**, which is the actual mechanism F3
   was written to probe (`REGISTRY_AND_MAPPING_PROPOSAL.md:221-226`). The
   inclusive answer is the one the referee question asks for, so this is not a
   gap in the deliverable — but it means F3 bounds the effect without testing the
   mechanism, and nobody should read it as having tested the mechanism.
4. **The merge has not launched for a fifth consecutive session.** Correct every
   time — the gate genuinely is not done — but five sessions of scaffolding is
   the honest description of where the effort has gone, and the extraction with
   SEMs is still two steps out.
5. **The gate's ETA has been stable across three independent frontier readings**
   (slot_100, 300, 400 → 22.14, 22.56, 22.53 s/dir). That is the one projection
   this arc that has held, and it held because the frontier is a direct
   measurement rather than an extrapolation from a prefix.

---

## 7. If you read only one thing

**F3 is complete and green: all twelve virtual species close at exactly 1, and
trigger-side completeness agrees — B* within 0.25 % of B, an order of magnitude
inside the ground states' own charge-state spread, and Σ_b* unresolvable at
n ≈ 30. The one-sentence trigger-side answer is supported. The gate is at
slot_400 of 3000 with a stable ETA of ~16:15 today, and the merge launches the
moment it PASSes.**
