# Handoff v26 — bimodality settled, sweep closed, sync NOT done

**Read v21 (`48cf3a3`) for generational context.**

| | |
|---|---|
| **Local HEAD** | **`ecfabab`** on `physics-focus` (+3 commits this session) |
| **Nikhef HEAD** | **`e6429b7`** |
| **DO THE HEADS MATCH?** | **NO. The sync did not happen** — see §5 |
| **Cluster** | **`5397565`** (retry round 3, 1 job, running) |
| **Campaign** | **2999 / 3000** |
| **Seed ledger** | **3557 / 3557 unique** |
| **Suite** | **27 / 27** at `ecfabab` |

> **THE FREEZE STILL APPLIES.** Round 3's single job verifies `e6429b7` at
> startup. It ends when that job promotes — not before.

---

## 1. BIMODALITY — settled, and it made a correct prediction

**Pre-registered:** a clean empty gap between each CR tune's completed maximum
and the 3600 s guard means genuine wedges; its absence means the guard clips a
real slow tail.

| tune | completed max CPU | guard-removed min | **GAP** |
|---|---|---|---|
| MONASH | 758 s | — (no holds ever) | — |
| JUNCTIONS | 1339 s | 3601 s | **2262 s** |
| CLOSEPACKING | 1597 s | 3601 s | **2004 s** |

**Zero completed jobs above 3000 s in any tune.** Two independent routes agree:
`condor_history` CPU over 3000 attempt-0 rows, and the producer's own
`elapsed_seconds` from sidecars (731/1316/1578 wall). Guard-removed jobs run at
CPU/wall **0.989–0.991** — spinning, not blocked, which is the wall≈CPU caveat
measured rather than assumed.

**The distribution is bimodal with an empty 2 ks band.** The guard is not
clipping a slow tail; there is none within 2000 s of it.

> **THE PREDICTION, made and confirmed.** Mid-session one job was still running
> at `RemoteUserCpu 2400 s` — 1061 s past JUNCTIONS' healthy maximum, inside the
> empty band. **I recorded the prediction that it was wedged and would hit the
> guard.** It was held ~20 minutes later at **3897 s**. The gap analysis has
> predictive power, not just descriptive: **a running CR job past ~1600 s CPU is
> already wedged and will not finish.** That is worth using operationally.

**This corrected my own v24 reading** that the last-25-all-hung pattern was CPU
exposure. Under exposure the completed distribution would approach the guard; it
stops dead at 1597 s. A wedged job runs until a guard kills it, so wedged jobs
are *always* last to leave a queue whatever the tune's speed — a selection effect
on **exit order**, not evidence about runtime.

**The mechanism remains unknown.** What is settled is that the guard is not
mis-firing, so its threshold needs no revision and every retried job was
correctly identified.

---

## 2. RETRIES — round 2 measured, round 3 flying

| round | retried | residual | pre-registered | held? |
|---|---|---|---|---|
| 1 | 119 | **7** | ~7 (6 % lottery) | ✅ exact |
| 2 | 7 | **1** | ~0–1 | ✅ inside |
| 3 | 1 | *in flight* | ~0 | cluster `5397565` |

Round 3 applied with every pre-registration matched: `missing == held == 1`,
job `JUNCTIONS:239`, seed **`131300240`** exactly as computed before rendering,
ledger 3556 → 3557 unique.

**The lottery model continues to hold.** Nothing so far contradicts ~6 % per
attempt per CR tune with a fresh draw each round.

---

## 3. THE SWEEP'S LAST ROW — closed, and the authorization went unused

**Pre-condition verified first**, as the authorization required: the config's
sha256 `9a86d7f8…` is pinned in **none** of 6720 provenance-bearing files under
the production root, and `statistical_robustness` appears in **no** promoted
provenance at all.

**Then it turned out no config edit was needed.** `statistical_robustness.py`
exact-matched `analysis_schema` against a value from its config; that check is
now **membership** in the pair-object contract's declared tags, fail-closed,
with the accepted set **derived** from the contract. Once the tool stopped
exact-matching, the config key is no longer consulted when judging data — it
survives only as the value a test uses to *construct* a v2 fixture, which is
correct. **`config/` is untouched.**

**Deliberately not swept:** the v2 literal in `validate_spec`'s
`expected_contracts` asserts what the **spec file** declares, not what a data
file carries. It is not a v3 gate. That is why the module is *not* in the
no-pin judge list — adding it would have failed on a legitimate line — and the
negative test pins the data path directly instead.

`docs/V2_PIN_SWEEP.md` is updated; **every row is now closed.**

---

## 4. FIRST CONTACT — LAUNCHED, RUNNING, **NOT HARVESTED**

The fourth-generation debt. The modified block closure is running **read-only
against real promoted v2 data**: central
`hadronization_merged/complete_root_HF_PT2_INT_MONASH` against its ten
`SUBSAMPLES_HF_PT2_INT/combined_root_subSamples_MONASH` blocks — 3300 ROOT
files, which is why it does not fit in a foreground ssh.

| | |
|---|---|
| **wrapper PID** | **`973959`** |
| ROOT PIDs | `982660`, `982662` |
| output | `/data/alice/ipardoza/first_contact/fc_out.txt` |
| sentinel | `/data/alice/ipardoza/first_contact/fc.done` (contains the exit code) |
| scratch tree | `/data/alice/ipardoza/first_contact/` |

**Pre-registered, and this is the whole point of the run:**

- exit **0**;
- `analysis_schema=paul_pair_objects_primary_ground_v2` reported by the macro;
- `object_content_sumw2_closure_checks=`**`1800`** (6 × 300) — **not 2100**;
- `invariant_metadata_checks=`**`600`** (2 × 300) — **not 1500**;
- `errors=0`, closure arithmetic unchanged;
- **promoted data byte-identical afterwards.**

**Baseline digests taken BEFORE the run**, so the last point is checkable and
not an assumption:

| set | name+size sha256 |
|---|---|
| `complete_root_HF_PT2_INT_MONASH` alone | `1d49d2f82802f6716b907a1e4a6f6f68bdd18e551eb4b6337746910d03c917ff` |
| central + all ten blocks | `9fb7aaadfb49816aa9d99325e286e501993b6e6ecc2ef615fb1e6bd165e97813` |

**Recompute both after and compare.** The command is
`{ ls $CENTRAL/*.root; for i in $(seq 1 10); do ls $BLOCKS/combined_root_$i/*.root; done; } | sort | xargs -I{} stat -c "%n %s" {} | sha256sum`.

**Check the sentinel before relaunching.** If those derived counts come back at
**v2** values, the version-aware machinery is proven against production rather
than only against fixtures — which is exactly the gate between "the tests pass"
and "production trusts it".

---

## 5. THE SYNC — NOT DONE. Pre-flight is done and clean.

**Blocked, correctly, on its own precondition:** the sync runs only at
**3000/3000 with zero in flight**, and round 3's job is still running.

**The pre-flight is complete and can be trusted next session:**

- **40 commits** to carry (`e6429b7..ecfabab`), 45 files, +7203/−155.
- **Producer/build check: CLEAN.** No `SimulationScripts/*.{cc,cxx,cpp}`, no
  `Makefile`, no `CMakeLists`, no `*.cmnd` tune card, no `setupEnv.sh` in the
  diff. Everything is analysis macro, validators, plotting, tools, the two
  authorized configs, docs and tests.
- **Pre-registered suite count, taken on the local machine as required: 27.**
- **Pre-registered producer SHA: byte-identical `e54b27bb9e3f…`.** Nothing this
  generation touched the producer's translation unit, so **a moved SHA is a
  SESSION-STOP.**

**Sequence when convergence lands:** verify `condor_q` empty by *full output*,
bundle, transfer, `merge --ff-only` on Nikhef, `make check` bare (expect 27),
producer rebuild (expect `e54b27bb…`).

---

## 6. B6 — NOT STARTED

Gated on the sync, which is gated on convergence. Its standing pre-condition is
unchanged: **verify the dataset-selector JSONs are not sha-pinned in any
provenance chain before adding the `hf_pt2_int` entry.**

---

## 7. POST-CAMPAIGN SEQUENCE — enumerated, not started

1. **Sync** (§5) — ends the freeze.
2. **First contact harvest** (§4) if not already closed.
3. **B6 discharge** (§6).
4. **Per-tune discard-bias bounds** at final rates, all attempts summed. **The
   2.7 % sentence is dead**; the replacements are per-tune, and the attempt-0
   numbers are 0.0 % / 6.0 % / 5.9 %.
5. **The v3 analysis campaign** (3000 jobs, contract env vars per
   `docs/SPECIES_AXIS_VALIDATION.md` §4).
6. **The gate** at 3000 directories under CPU-bound sizing — batch node,
   pool per the measured **440.9 MiB/child**.
7. **THE SPECIES-OBJECT SCALING MEASUREMENT — before any full merge.**
   10 / 25 / 50 real v3 inputs, retention rule in force. **The full merge does
   not run until `hFlavourClosureSpecies` is priced.** The 49× cliff on
   `hFlavourClosure` has no surviving mechanism, and the species object is a
   202-bin axis where that one is 6 — it must not be extrapolated.
8. **The merge**, per-directory invocation, 32 h per-process ceiling watched.
9. **Closure**, then **the v3-through-plotting exercise** that discharges the
   last untested-claim debt.

---

## 8. COLD-READ SELF-REVIEW

1. **No v3 data has been through the plotting layer.** Unchanged, and now the
   *only* remaining structural unknown in the v3 chain — the sweep is closed, so
   the gates accept v3; nothing has exercised them.
2. **First contact is unharvested** (§4). Until its numbers are read, the
   version-aware machinery is still proven only against fixtures. **Do not treat
   §3's "every row closed" as meaning production has accepted it.**
3. **My convergence monitor reported a false QUEUE_EMPTY.** During an SSH
   outage `condor_q` failed, my `awk` summed an empty result to 0, and the loop
   declared the queue drained. **It conflated "no jobs" with "couldn't ask".**
   Nothing was acted on — I checked state before proceeding and found the job
   still running — but a monitor that cannot distinguish those two is unsafe to
   gate an irreversible step on. **Fix it before using this pattern for the
   sync.**
4. **The cluster had an SSH outage** of several minutes mid-session
   (banner-exchange timeouts). It recovered on its own. Worth knowing if the
   sync fails oddly.
5. **I did not run `ValidatePairDirectory` itself** — only the block closure.
   The closure exercises version resolution, both derived counts and the
   identity set, which is the richer test, but the allowlist's exact-match arms
   on merged data are still unexercised against production. **Run it too.**
6. **The 12-vs-10 follow-up is recorded but not done** (a new contract axis for
   the mode-dependent relation). Second generation for that one now.
7. **`Validation/TestPlotProjectionCuts.C` still has no v3 fixture case.** The
   plotting gate's v3 path is unit-tested only through the parity test.

---

## 9. If you read only one thing

**Harvest first contact (§4) — PID `973959`, sentinel `fc.done`, and the
pre-registered counts are 1800 and 600, not 2100 and 1500. Then sync (§5), whose
pre-flight is done and clean: 40 commits, no producer or build changes, expect
27/27 and `e54b27bb…`. A running CR job past ~1600 s CPU is already wedged.**
