# Handoff v17 — v2 seeds confirmed live, B4 launched, stage-1 not started

**Date:** 2026-08-09. **Branch:** `physics-focus`.
**HEAD at time of writing:** `918ae18` (local).
**Nikhef HEAD:** `e6429b7` — **FROZEN, unmoved, and it stays there.**

> ### CHECKOUT FREEZE STILL IN FORCE — HF_RUN3_V1 IS IN FLIGHT
> Jobs verify their commit at startup. **Moving the Nikhef checkout would fail
> every job that has not yet started — currently ~2866 of 3000.** Local commits
> accumulate; anything running on Nikhef with new code uses the scratch-deploy
> pattern.

---

## 1. Campaign status — healthy, no retries earned

| | |
|---|---|
| Cluster | `5390385`, released 2026-08-09T04:23:55Z |
| Queue at last snapshot | **2866 idle, 72 running, 0 HELD** |
| Promoted | **MONASH 62**, JUNCTIONS 0, CLOSEPACKING 0 |
| Validator failures | **none** |

**Queue read by REASON, not count** — `condor_q -constraint "JobStatus==5"`
returns nothing. **Zero holds of either class.**

**Retry round 1: NOT EARNED, not run.** No hang-guard holds exist.
**Their absence this early is uninformative, not reassuring** — the guard fires
only after >3600 s CPU, and MONASH's mean job is ~377 s, so nothing has been
alive long enough to trip it. `resubmit_held.py` remains untested beyond
batch-of-1; that test is still ahead.

**Tune ordering:** jobs 0–999 are MONASH, so JUNCTIONS and CLOSEPACKING have
not started. That is expected, not a fault.

---

## 2. THE DEFERRED CHECK IS DONE — `seed_derivation_v2` is confirmed live

**First confirmation that v2 reached the jobs. Everything before this was
pre-submission checking.**

```
PRODUCTION_START campaign=HF_RUN3_V1 tune=MONASH logical_id=0 role=primary attempt=0 seed=130000001
PRODUCTION_START campaign=HF_RUN3_V1 tune=MONASH logical_id=2 role=primary attempt=0 seed=130000003
PRODUCTION_END   attempts=100000 successful_events=100000 failed_attempts=0 tree_entries=100000
```

`attempt_metadata/…job000_attempt000_5390385_0.json` carries
**`campaign_ordinal: 3`**, `seed: 130000001`, `attempt: 0`.

**`130000001` is exactly `SEED_BASE + 3*CAMPAIGN_STRIDE`**, hand-computed before
submission; `logical_id=2 → 130000003` increments correctly; **attempt 0**
confirms the axis B15b freed is being used as intended.

Promoted `hf_MONASH_job000.root` is 92,200,782 B (88 MiB, matching budget) with
`.sha256` sidecar and write-once mode.

**STILL OUTSTANDING — and it matters more than it looks.** JUNCTIONS and
CLOSEPACKING are unverified because they have not started. **MONASH alone
cannot exercise the tune-index term of the v2 formula.** When they start,
check: **JUNCTIONS job 0 ⇒ `131000001`**, **CLOSEPACKING job 0 ⇒ `132000001`**.

---

## 3. B4 — smoked and launched

**Deploy (scratch pattern, checkout untouched):**

```
/data/alice/ipardoza/b4_mapping/
  macro/CalibrateMultiplicityAgainstMinBias.C
  SimulationScripts -> $PROJECT/SimulationScripts   (relative-include trap)
  run_b4.sh  logs/  out/  run_meta.txt
B4_SOURCE_COMMIT = 884f76e
B4_MACRO_SHA256  = 3be7a09422e734ec68ae7d2fab279b2d055158b0a2dd29f201bbe13192192cf1  (matches expected)
B4_NIKHEF_CHECKOUT = e6429b7   (unmoved)
```

**`run_b4.sh` invokes the DEPLOYED macro explicitly**, never `$PROJECT`'s copy,
which under the freeze is the pre-extension version.

### Smoke results — the never-executed code now has run

| check | result |
|---|---|
| unreadable card ⇒ return 6, no silent Monash fallback | **PASS** — `ERROR: cannot read tune card …`, `(int) 6` |
| card read | **PASS** — `B4_TUNE_CARD=…ccbb_MONASH.cmnd label=MONASH` |
| `TH1D` fills and writes on the analysis axis | **PASS** — `hNch_mb_MONASH` 8642 entries, `hNch_hard_MONASH` 9888 entries, **overflow 0** both |
| MB arm is genuinely MB | **PASS by inference** — see below |

**On the MB check, stated precisely because it is inference not direct read.**
The macro does not echo PYTHIA's settings table, so `HardQCD` state could not be
read directly. The evidence is **MB ⟨N_ch⟩ = 13.19 against hard ⟨N_ch⟩ = 11.17**
— an 18 % separation at ~9k events, far beyond statistics, and **lower for
hard**, matching the direction and rough size of §3.12's measured pTHatMin
table (−4.2 % at 2.0). **Had `HardQCD` been left on in the MB arm the two means
would coincide.** A settings echo would make this direct; worth adding.

### Six runs launched, detached, 200k events each

```
PID 477910 MONASH hard      PID 478064 MONASH mb
PID 478127 JUNCTIONS hard   PID 478222 JUNCTIONS mb
PID 478315 CLOSEPACKING hard PID 478474 CLOSEPACKING mb
B4_LAUNCH_UTC=2026-08-09T04:49:23Z    all six alive at last check
```

**All six confirmed `events=200000`** from their own `B4_RUN_START` lines — an
earlier PID listing showed `20000` for one, which was `cut -c1-70` display
truncation, not a mangled argument. Launched uncontended (loadavg 0.72 / 62
cores). Outputs land in `b4_mapping/out/nch_{arm}_{TUNE}.root`.

### HARVEST CONTRACT — the difference check runs FIRST

> **Before evaluating the ±3 pp agreement criterion, assert the three tunes'
> outputs are NOT identical** — per-tune MB ⟨N_ch⟩ must differ beyond
> statistics. **Identical outputs ⇒ item-STOP: the card plumbing failed again.**
>
> **An agreement gate is meaningless until the difference check passes.** Three
> identical MONASH samples would agree at 0.0 pp on all 11 boundaries and sail
> through the criterion. This is the institutionalised form of the
> `Tune:pp = 14` catch from v16.

Each run records its card path (`B4_TUNE_CARD`) and mean
(`NCH_DISTRIBUTION_WRITTEN`), which is sufficient for the check.

**Pre-registered acceptance, unchanged:** all 11 boundary→MB-percentile mappings
within **±3 pp** across tunes ⇒ MB-anchored convention proceeds; any outside ⇒
owner escalation with the full table, no convention change.

---

## 4. Item 3 — stage-1 first slice: NOT STARTED

Context, and the brief's priority is 1 > 2 > 3. Nothing begun, nothing
half-done. The slice is unchanged: raw fixture by `scp` with sha256 recorded;
species ordinal artifact from the fixture's hashed `heavy_stability_audit`;
`hFlavourClosure` species axis behind schema v3; local validation (closure sum
rule to 1e-9, species-axis totals reproducing the 6-category axis bin-for-bin);
F5 supersession policy drafted as text for owner review.

---

## 5. Cold-read self-review

1. **The campaign is healthy on 62 of 3000 jobs.** MONASH-only, ~2 % complete.
   **No conclusion about JUNCTIONS or CLOSEPACKING is available**, including the
   v2 tune-term check, which is the one part of the seed formula still
   unverified against live data.
2. **`resubmit_held.py` is still untested on a batch.** The first real test is
   whenever hang holds appear, and it has not happened.
3. **The MB/HardQCD check is inference from ⟨N_ch⟩, not a settings read.** It is
   strong inference — 18 % separation in the predicted direction — but I could
   not read the flag. **A settings echo in the macro would close it properly.**
4. **The six B4 runs have produced no output yet.** Launch is confirmed; results
   are not. The difference check in §3 is the guard against believing them
   prematurely.
5. **I did not touch the queue** beyond read-only queries — no retries were
   earned, so `resubmit_held.py` was not run.
6. **Nikhef checkout unmoved at `e6429b7`;** everything new went to
   `/data/alice/ipardoza/b4_mapping/` scratch with sha256 and source commit
   recorded.
