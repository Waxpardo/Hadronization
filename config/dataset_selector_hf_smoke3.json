{
  "schema": "hadronization_dataset_selector_v1",
  "active_dataset": "hf_smoke3",
  "datasets": {
    "hf_smoke3": {
      "analysis_root": "${HADRONIZATION_DATA_ROOT}/hadronization_analysis/HF_SMOKE3",
      "analyzed_data_base": "${HADRONIZATION_DATA_ROOT}/hadronization_merged",
      "block_count": 10,
      "campaign": "HF_SMOKE3",
      "canonical_manifest": "${HADRONIZATION_DATA_ROOT}/project/runs/HF_SMOKE3/freeze/canonical_manifest.jsonl",
      "complete_root_tag": "complete_root_HF_SMOKE3",
      "interpretation": "HF_SMOKE3 is a 600,000-event operations-certification campaign, not physics: three tunes, 10 jobs per tune, 20,000 requested successes per job. It exists to run generation, analysis, merge, freeze and plotting end to end from the rebuilt repository, whose planes until now rested on contract tests and on reuse of pre-rebuild bytes. Ten jobs per tune fill the ten file blocks exactly once (canonical_slot % 10, PRACTICE 5.1), which is the smallest exposure the canonical manifest will accept. It is a canonical_candidate so the merge-consuming plotting path admits it; publication_eligible is false and no authorization is cited, deliberately. PRACTICE 2.7 two-tier rigor applies: its numbers carry ordinary engineering care and no measurement discipline, and nothing derived from it may ever become publication-eligible. The data routes are resolved through HADRONIZATION_DATA_ROOT by the selected site profile. The combined selector names no default, so this dataset must be named: --dataset hf_smoke3, HADRONIZATION_DATASET=hf_smoke3, or the single-row file config/dataset_selector_hf_smoke3.json.",
      "production_root": "${HADRONIZATION_DATA_ROOT}/hadronization_production/HF_SMOKE3",
      "publication_authorization": null,
      "publication_authorization_sha256": null,
      "publication_eligible": false,
      "raw_base": "${HADRONIZATION_DATA_ROOT}/hadronization_production/HF_SMOKE3/raw",
      "raw_schema": "hf_primary_ground_raw_v7",
      "selector": "hard_trigger_primary_ground__primary_ground_associate_v1",
      "status": "canonical_candidate",
      "subsample_base": "${HADRONIZATION_DATA_ROOT}/hadronization_merged/SUBSAMPLES_HF_SMOKE3/combined_root_subSamples"
    }
  }
}
