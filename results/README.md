# Historical results

The numerical trees `systematics/20260819/` and `systematics/20260820/` use the
retired common absolute multiplicity axis. They are historical provenance
only, are prohibited as current or publication results, and are not current
result roots. Their machine-readable scope records are:

- `systematics/20260819/RETIREMENT_STATUS.json`
- `systematics/20260820/RETIREMENT_STATUS.json`

The surviving tune-local class contract is
`../config/multiplicity_percentile_classes_v2.json`. Its application and
measurement-path checks live in
`../tools/apply_per_tune_multiplicity_contract.py` and
`../extraction/harvest_class_axis.py`. The historical numerical artifacts stay
byte-preserved until the J-b/J-c tune-local successors exist.
